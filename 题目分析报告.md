# 2026 年全国大学生数学建模竞赛 C 题分析报告

> 版本：2026-09-11 建模改进设计。范围仅为建模手：更新模型合同和术语，说明意见取舍；不改求解代码、既有结果、论文和 Checkpoint-V1。本文的新方案尚未完成年度联合闭环回测，不能当作已验证结果。参赛队仍须核验原题、规则和所有数值。

## 1. 任务重述与总目标

题目要求在 10 min 时间粒度下，为含光伏和电化学储能的小区微网制定外部电网购电计划。四问逐步引入三类不确定性：实际负荷与实际光伏偏离事前估计、日内光伏预测更新、逐日逐时电价变化。核心矛盾是：少买会触发 5 倍电价的应急购电，多买又可能形成无效购电或付出向下调整违约金；储能可在时段之间搬移能量，但受容量、功率和效率限制。

总目标是在任何时段均满足负荷、储能物理约束和信息可得性约束的前提下，最小化计划购电费、日内调整费和应急购电费，并输出题目指定的 5 份结果工作簿（result1、result2、result3、result4-2、result4-3）、典型日表格、费用分解和更新时刻建议。

### 1.1 本次采用的主线与模型数量

四问继续按照“确定性调度 → 预测与风险 → 日内信息和调账 → 时变电价”递进，不分别另造四套系统。改进顺序是：先修正中心预测，再重新估计安全余量，最后让储能执行兼顾未来费用。

主线：**周期型因果点预测 → 净负荷残差分位修正 → 合同 LP → 短时滚动储能 LP → 统一现金结算**。完整历史误差路径回放和 CVaR 是验证指标，不新增一个正式随机优化决策器。

| 问题 | 决策/结论 | 本轮独立模型系统 | 后问复用关系 |
|---|---|---|---|
| Q1 | 典型日最优购电、充放电与费用 | 1 个：确定性 LP | 提供全部物理和现金约束 |
| Q2 | 未知实际负荷/光伏下的合同、执行及风险 | 至多 2 个：历史周期统计预测；统一物理 LP（合同与执行两个时域） | 增加残差校准和真实闭环回测 |
| Q3 | 日内调整合同和信息价值 | 同上 2 个；官方光伏预报是外生输入，偏差校准是同一统计预测的修正 | 继承新负荷预测、风险和执行器，新增合同调整约束 |
| Q4 | 变价下两种购电模式及价格信息损失 | 同上 2 个；同槽价格均值/中位数仍为历史统计预测的候选规则 | 只推广价格信息集和结算价 |

候选统计规则是相互替换的方案，不串联成集成模型；风险分位是预测输出的校准，不另设学习器。完整路径回放只执行既定策略并统计风险。两阶段 SAA 若作为独立优化器加入，会增加另一套决策系统，因此本轮只保留研究接口并暂缓实施，不能靠把它改名“验证”绕过模型数量限制。

## 2. 输入、数据审计与时间口径

### 2.1 文件与数据规模

| 文件 | 主要内容 | 规模 | 用途 |
|---|---:|---:|---|
| 附件 1 | 典型日电价、负荷、光伏预测功率 | 144 个 10 min 时段 | 问题一；问题二的固定日内电价 |
| 附件 2 | 2025 年实际负荷、实际光伏功率 | 365×144，各 2 张表 | 问题二至四回测真值 |
| 附件 3 | 每日 0/6/12/18 时发布的未来 24 h 光伏预测 | 365×4×24 | 问题三、四滚动更新 |
| 附件 4 | 2025 年 10 min 电价 | 365×144 | 问题四 |
| 附件 5 | result1/2/3/4-2/4-3 模板 | 固定表名和表头 | 最终结果写入 |

原始文件保持只读，输出使用模板副本。

### 2.2 审计结果

以下细部量值沿用既有报告的附件审计，不声称本轮全部重算。本轮额外只读检查了全部工作簿表名、行列规模，并用实际附件复算了第 6.1 节两个周期预测器的误差。

1. 附件 1 有 144 个唯一时刻；电价、负荷和光伏均无缺失、无负值。电价范围为 0.3713–1.3952 元/kWh，日负荷电量为 111024.8081 kWh，日光伏预测电量为 55482.8357 kWh。
2. 附件 2 的日期连续覆盖 2025-01-01 至 2025-12-31，两个工作表均为 365×144；无缺失、无负值。负荷功率范围为 1995.7176–7978.8849 kW，光伏功率范围为 0–10216.2 kW。
3. 附件 3 每天恰有 0:00、6:00、12:00、18:00 四次预测，每次 24 个小时值；无缺失、无负值。日期单元格按原表向下填充，不把空白日期当作缺测。
4. 附件 4 的日期、144 个时刻与附件 2 完全对齐；电价范围为 0.0076–1.7936 元/kWh。
5. 附件 1 的负荷、光伏、电价曲线分别近似等于附件 2、2、4 的逐时年度均值，最大逐点差分别约为 `5.04e-5 kW`、`0.0431 kW`、`5.15e-5 元/kWh`，因此可把附件 1 解释为“年度典型均值日”。
6. 附件 3 的历史预测误差随提前期增加而增大：1 h、6 h、12 h、18 h、24 h 提前期的光伏 MAE 约为 18.69、89.93、159.71、224.32、374.79 kW。这为日内滚动更新提供了直接数据依据。

极端高负荷和高光伏值在相邻时段连续出现，且均满足非负性与物理量级，不作为异常值删除。后续仅对预测值做非负截断，不修改真实值。

### 2.3 10 min 时段映射

输入表的 144 个标签从 `00:10` 至次日 `00:00`，可自然解释为每个 10 min 时段的结束时刻。因此计算时令第 \(t\) 个数对应自然日区间 \([(t-1)10,t10)\) min：第 1 行是 0:00–0:10，第 144 行是 23:50–24:00。全部能量由

\[
\Delta t=\frac{1}{6}\ \mathrm{h},\qquad E=P\Delta t
\]

换算。题目给出的结果模板却把第 1 个输出列写成 `0:10-0:20`，比原始数据的自然区间右移 10 min。程序不改动官方模板表头，按原始数据行序逐列填入，并在论文中明确该表头不一致；所有 SOC、0–4 h 聚合和指定时刻抽取均以自然日计算索引为准。另以“模板表头作为开始时刻”的整体循环平移映射重算典型日，作为边界敏感性检验。

## 3. 模型假设

1. 外部电网购电和应急购电均无额外功率上限；若实际存在并网容量上限，应把该上限加入约束后重算。
2. 光伏可弃光且无直接罚金，外购电不可反向售电。已经支付但未消纳的计划购电记为无补偿弃购，不抵扣费用。本轮保守解释“低于负载时紧急购电”：应急电只补当槽负荷缺口，不主动给储能充电，也不为终端估值套利；普通合同购电仍可用于充电。预测落空时先缩减原定充电，具体限制见第 7.2—7.3 节。若未来放宽应急用途，必须单独作为操作权限敏感性，不把收益全部归于 MPC。
3. 题目给出的 90% 储能效率同时用于充电和放电，即 \(\eta_c=\eta_d=0.9\)。另以“90% 为往返效率，即 \(\eta_c=\eta_d=\sqrt{0.9}\)”进行敏感性检验。
4. 充、放电功率上限均为 5000 kW；单个 10 min 时段的能量上限为 \(833.3333\) kWh。\(x\) 表示电池交流侧吸收量、\(y\) 表示电池交流侧送出量，两者都受该上限约束；SOC 分别增加 \(\eta_cx\) 和减少 \(y/\eta_d\)。电量边界为 1200–10800 kWh，额定容量为 12000 kWh。
5. 问题一按题意取 \(S_1=S_{145}=6000\) kWh。问题二至四从 2025-01-01 0:00 的 6000 kWh 开始逐时连续传递 SOC，不强制每日首末相等。每次有限时域优化使用终端储能价值防止末端透支，12 月 31 日末仍按同一价值计量并单列期末 SOC。
6. 旧执行器为单向贪心；新设计另引入第 7 节滚动 LP，不能预先声称新执行器已通过互斥检查。计划层不预设“不会同时充放电”：先最小化经济费用，再在最优费用容差内最小化总充放电量，并逐次记录同时充放电槽数及最大重叠量。贪心基线结构上不会同时充放电；新滚动 LP 必须同样接受互斥与实际动作检查。第 5 节给出连续 LP 无须立即改为 MILP 的充分条件与失效边界。
7. 在 0:00 制定计划时，只使用该日以前的实测数据和题目事先提供的典型日资料、当天已发布的光伏预测；在 6/12/18 时调整时，只使用当时已经发布的预测和已经实现的观测，禁止使用未来实际值。附件 1 虽与全年度均值接近，但作为题设外生先验使用，不能称其为由纯历史训练得到的量。问题四另按第 9 节预测电价。

假设分级：无外网购电上限、允许无补偿弃购/弃光、应急电仅补负荷缺口、上述信息集为四项关键边界；若改变，必须重构可行域或信息集并重新回测。效率解释、初末状态与终端价值、吞吐次级优化是可检验的实现设定，分别用效率替代、末态披露与物理残差检验复核。

## 4. 符号与储能—功率平衡模型

设一天含 \(T=144\) 个时段，历史误差情景集合为 \(\Omega\)。所有购电、充放电变量均按每时段 kWh 计。为避免把“规划最优动作”误写成“真实执行动作”，以下显式区分规划层上标 \(\mathrm P\) 与执行层上标 \(\mathrm E\)。

- \(L_t,V_t\)：负荷与可用光伏电量；
- \(p_t\)：0:00 确定的计划购电量；
- \(q_t^{(r)}\)：第 \(r\) 次更新后的合同购电量；
- \(c_t\)：电价；
- \(x_t^{\mathrm P},y_t^{\mathrm P},S_t^{\mathrm P},v_t^{\mathrm P}\)：规划 LP 的充电、放电、SOC 与利用光伏；
- \(x_t^{\mathrm E},y_t^{\mathrm E},S_t^{\mathrm E},v_t^{\mathrm E}\)：合同生效后按真实观测得到的执行量；
- \(u_t,w_t\)：执行／结算层的应急购电和已付费但未消纳合同量，不是现有 `solve_schedule` 的决策变量；第 7 节新执行 LP 将另设预测补救量，须与真实记账量区分。

规划 LP 的物理约束为

\[
g_t+v_t^{\mathrm P}+y_t^{\mathrm P}=\widetilde L_t+x_t^{\mathrm P},
\]

\[
S_{t+1}^{\mathrm P}=S_t^{\mathrm P}+\eta_c x_t^{\mathrm P}-\frac{y_t^{\mathrm P}}{\eta_d},
\]

\[
1200\le S_t^{\mathrm P}\le10800,\quad 0\le x_t^{\mathrm P},y_t^{\mathrm P}\le833.3333,
\]

\[
0\le v_t^{\mathrm P}\le \widetilde V_t.
\]

同时要求合同 g_t≥0；执行记账满足 u_t≥0、0≤w_t≤g_t。所有状态（包括末端）均接受上下界检查。

其中 \((\widetilde L,\widetilde V)\) 是当次决策可得的确定性或分位安全轨迹；问题一、二令 \(g=p\)，问题三、四令 \(g=q^{(r)}\)。真实执行另满足

\[
g_t-w_t+v_t^{\mathrm E}+y_t^{\mathrm E}+u_t=L_t+x_t^{\mathrm E},
\qquad
S_{t+1}^{\mathrm E}=S_t^{\mathrm E}+\eta_cx_t^{\mathrm E}-y_t^{\mathrm E}/\eta_d,
\]

以及同样的 SOC、功率、光伏利用与非负边界。问题一直接采用规划解，不需要应急与弃购记账；问题二至四把执行层真实末态传给下一时段和下一日。问题一才有 \(S_{T+1}^{\mathrm P}=S_1^{\mathrm P}\) 的日电量循环约束。

## 5. 问题一：确定性线性规划

附件 1 给定完整的典型日电价、负荷和光伏预测，因此采用确定性线性规划：

\[
\min \sum_{t=1}^{T} c_t p_t.
\]

约束为第 4 节规划模型，并令 \(g_t=p_t\)、\(S_1^{\mathrm P}=S_{T+1}^{\mathrm P}=6000\)。若光伏超过负荷与储能可吸收量，通过 \(v_t^{\mathrm P}<V_t\) 表示弃光。由于模型是连续线性规划，可用 HiGHS 获得全局最优解。随后在最优费用增加不超过 \(10^{-8}\max(1,|C^*|)\) 的条件下最小化 \(\sum_t(x_t^{\mathrm P}+y_t^{\mathrm P})\)，减少无效循环；该次级目标不保证调度唯一，仍须逐时检查同时充放电量。

在本题无售电、充放电效率满足 \(0<\eta_c\eta_d<1\)、光伏允许无偿弃置、充放电没有负成本且其他约束只通过净供能与 SOC 起作用时，若某槽同时有 \(x_t^{\mathrm P},y_t^{\mathrm P}>0\)，可沿保持 SOC 变化量的方向削减二者，并通过减少外购电或增加弃光恢复母线平衡，从而不增加经济目标且严格减少吞吐。因此存在互斥的经济最优解，二阶段 LP 会选择该类解。若允许售电、出现负电价／吞吐补贴、设置最小开机量或新增路径约束，该论证可能失效；此时才需要重新审核互补约束或 MILP。实现验收仍以每个规划解 `min(charge,discharge)>1e-5 kWh` 的槽数为 0、最大重叠量小于阈值为准。

问题一还提供无需求解器的日电量恒等式。记全天规划充、放电总量为 \(X=\sum_tx_t^{\mathrm P}\)、\(Y=\sum_ty_t^{\mathrm P}\)、购电为 \(G=\sum_tp_t\)。循环 SOC 给出

\[
Y=\eta_c\eta_dX=0.81X,\qquad
G=\sum_tL_t-\sum_tV_t^{\mathrm P}+(1-0.81)X.
\]

当最优解无弃光时，代入附件 1 与现有解可复算为 \(111024.8081-55482.8357+0.19\times20740.6618\approx59482.6982\ \mathrm{kWh}\)。该恒等式把 SOC 循环、效率损耗与总购电量连成独立物理校验；若重算后出现弃光，则应把未利用光伏量加回右端并据实更新数值。

输出：144 点计划购电量，4 h 聚合充放电量与 0/24 时储能电量，总购电量和总费用，并填充 `result1.xlsx`。


## 6. 问题二：周期预测与经济风险分层

### 6.1 为什么改预测器

先用数据决定预测结构，再用调度费用决定是否采用。评审提出的短周期方法在本题数据上已有明显误差优势。本轮用 pandas/NumPy 对实际附件重新计算 F1、F2，评价期为 2025-02-01—12-31：

| 候选 | 负荷 MAE/kW | 光伏 MAE/kW | 净负荷 MAE/kW | 净负荷 RMSE/kW |
|---|---:|---:|---:|---:|
| F0：旧 90 日加权模型（评审已有结果，本轮未重算） | 722.57 | 335.92 | 804.08 | 1074.83 |
| F1：上周同槽 + 7 日光伏均值（本轮复算） | 176.7957 | 153.3997 | 273.3513 | 389.9670 |
| F2：三周同星期中位数 + 7 日光伏均值（本轮复算） | 187.8987 | 153.3997 | 284.3536 | 403.2466 |

F1 是第一候选，F2 用于抵抗异常周，F0 是旧版基线；点预测误差领先不等于调度费用已领先。关于周周期的解释还需比较去除平均日内曲线后的 1/7/14 日相关性、分月误差和异常周，不能仅用原始序列高相关系数作证明。光伏短窗可能减少季节错配，应由分月回测支持；非对称尾部也需用残差分布验证，不能预设。

### 6.2 可实现的候选定义

F0 保留最近 90 日历史，权重为 exp[-(d-j)/60]，负荷同星期额外乘 2，光伏不乘星期权重。加权均值与附件 1 典型日按 ρ=min(1,n/21) 收缩；没有历史时直接用典型日。它与当前代码的预测规则一致，只作基线。

F1、F2 定义为

\[
\begin{aligned}
\mathrm{F1}:\quad &\widehat L_{d,t}=L_{d-7,t},\qquad
\widehat V_{d,t}=\frac17\sum_{j=1}^{7}V_{d-j,t},\\
\mathrm{F2}:\quad &\widehat L_{d,t}=\operatorname{median}\{L_{d-7,t},L_{d-14,t},L_{d-21,t}\},
\quad \widehat V_{d,t}=\frac17\sum_{j=1}^{7}V_{d-j,t}.
\end{aligned}
\]

启动规则固定如下：F1 没有 d−7 时，负荷取所有已完成历史日的同槽均值；F2 只使用存在的 d−7/14/21，若一个也没有则使用同样的历史均值。无历史均回退附件 1。光伏不足 7 日取已有最近日均值，完全无历史用附件 1。该规则在 kW 上实现后乘 Δt 进入优化。

本轮不增加加权三周、EWMA、回归或树模型。若后续增加，必须先明确权重、冷启动和冻结时点，不用一张含糊的“加权预测”表代替公式。

### 6.3 中心预测与风险余量分开

中心预测是 \(\widehat N=\widehat L-\widehat V\)。每个候选各自保存当日真正发出的预测，日末完成后写入残差

\[
e^N_{j,t}=(L_{j,t}-V_{j,t})-(\widehat L_{j,t}-\widehat V_{j,t}).
\]

残差必须按“预测器版本—发布时间—日期—目标槽”保存。不能更换预测器后复用 F0 的残差，不能用当前模型倒填历史预测。完整配对误差 eL、eV 也保留，供第 10 节压力测试。

用日前最近 W 个已完成日期的同槽残差构造

\[
\widetilde N_{d,t}=\widehat N_{d,t}+Q_\alpha\{e^N_{j,t}:\max(1,d-W)\le j<d\}.
\]

有限样本等权分位取排序后第 ceil(αn) 个值；不插值。空残差库取修正 0，单条取其本身。空库只是启动规则，不表示没有不确定性。主候选 W=30，比较 21/30/45；保留旧 60 日配置用于历史对照。当前代码正式分支是未聚类、α=0.8，9 类 K-means 只是已有消融，不能把旧报告的“聚类主线”继续沿用。

评审已有旧闭环结果：W=60 时现金费 18446381.69 元，应急 499461.62 kWh；W=30 时费用 16782900.69 元，应急 228482.14 kWh，节省约 9.02%，334 天有 248 天、11 个月有 10 个月更省。这是缩短窗口的依据，本轮未重算年度费用，且该收益不能与预测、分位、MPC 的收益直接相加。

为保留弃光变量，唯一重构规则为

\[
\widetilde V=\max\{\widehat V,-\widetilde N,0\},\qquad
\widetilde L=\widetilde V+\widetilde N.
\]

它保证非负且净负荷不变，但这对负荷/光伏是合成的规划输入，不是物理联合情景；特别是 Vtilde 大于 Vhat 时应披露。旧版另取光伏低分位的构造只保留为单独消融，不能暗中混用。实际执行始终受真实可用光伏约束。

### 6.4 安全分位的经济依据

单槽无储能近似中，普通购电为 c，应急价为 5c，损失为

\[
\ell(p,N)=cp+5c(N-p)^+.
\]

连续分布内点的一阶条件为 c−5c Pr(N>p)=0，所以 α0=1−1/5=0.8；对离散经验分布按分位最优性条件取上述左逆分位，并受 p≥0 约束。也可解释为少买的额外边际损失 4c、多买损失 c，临界分位 4/(4+1)。

0.8 是理论基线，不是整日 80% 可靠度，也不保证含储能、终端价值和调账时最优。比较 α=0.8/0.9/0.95：评审已有旧配置费用分别为 18446381.69、17820050.08、17874943.66 元。故 0.9 是下一轮费用型第一候选；0.95 是减少应急的候选，而非预先确定的风险最优。新预测器、30 日、0.9 和滚动执行器尚未联合验证，必须重新选型。

### 6.5 合同层 LP 与现金结算

以当天真实起始 SOC 求第 4 节 LP：

\[
\min\sum_t c_tp_t-\lambda(S_{T+1}-S_{\min}),\qquad
\lambda=\eta_d\operatorname{median}(c^{\mathrm{known}}_{\mathrm{remaining}}).
\]

其中已知价在 Q2 为典型日价，在 Q4 为当时预测价。终端项单位为元，不读取次日真价，也不是现金收入。Q2 实际现金费用只记

\[
C_2=\sum_t c_tp_t+5c_tu_t.
\]

不能在普通合同无上限的单条轨迹 LP 中简单添加可自由替代的 5 倍应急变量并声称“联合优化风险”：优化器会用便宜合同替代应急。主线通过 α 的经济依据以及闭环现金选型考虑应急；第 7 节执行 LP 因合同已经固定，5 倍应急项才有真实决策意义。

## 7. 储能执行：短时滚动 LP 的完整合同

### 7.1 时序与信息集

在时段 t 开始时，信息集包含此前已完成的实测槽 1…t−1、当前 SOC、已生效合同、当时获准发布的预报和历史预测误差。附件标签代表区间结束，因此当前槽实际 L_t、V_t 不能进入本次 LP。区间内快速供需平衡另建执行步骤，不能把它称为提前得到的预测信息。

主实验取 6 h 视野 H=36；12 h（H=72）作对照。为不虚构次日合同，实际求解段为 t…K，K=min(t+H−1,144)，到午夜截断，使用终端价值处理边界；各方案下一日重新签约并连续传递 SOC。日前已签合同固定，日内也只允许第 8 节规定的合同事件更新。

执行预测使用最新获准版本的中心负荷/光伏，不重复加日前高分位余量；当前实测只用于状态与下一次允许的修正。Q2 默认不再增加一个新的短临预测器；Q3 的官方预报和 Q4 的价格修正按后文规则。

### 7.2 每槽求解的 LP

对 τ=t…K，gτ 是固定合同，barL、barV、barc 是当时预测。决策为 xM、yM、vM、uM、wM、SM、aτ，满足

\[
\begin{aligned}
g_\tau-w^M_\tau+v^M_\tau+y^M_\tau+u^M_\tau&=\bar L_\tau+x^M_\tau,\\
S^M_{\tau+1}&=S^M_\tau+\eta_cx^M_\tau-y^M_\tau/\eta_d,\\
S^M_t&=S^E_t,\\
0\le x^M_\tau,y^M_\tau\le B_{\max},\quad
S_{\min}\le S^M_\tau&\le S_{\max},\\
0\le v^M_\tau\le\bar V_\tau,\quad u^M_\tau\ge0,\quad 0\le w^M_\tau&\le g_\tau,\\
a_\tau\ge S^M_{\tau+1}-S^{target}_{\tau+1},\quad
a_\tau&\ge-S^M_{\tau+1}+S^{target}_{\tau+1},\quad a_\tau\ge0.
\end{aligned}
\]

为禁止用应急电主动充电，在求解前由固定合同与中心预测计算两个常数：

\[
r^+_\tau=\max(g_\tau+\bar V_\tau-\bar L_\tau,0),\qquad
r^-_\tau=\max(\bar L_\tau-g_\tau-\bar V_\tau,0),
\]

并增加 \(x^M_\tau\le r^+_\tau\)、\(u^M_\tau\le r^-_\tau\)。这些上限不包含决策变量，因此仍是 LP。预测有盈余时应急为 0，预测有缺口时充电为 0；电池可选择少放电、留电给未来，但不能额外买应急电存入电池。零充放电配合应急或弃置总能满足当槽平衡，故在无并网限额的假设下此限制不破坏基本可行性。互斥仍按次级吞吐和数值门禁检查。

末端状态同样受上下界约束，目标是

\[
\min\sum_{\tau=t}^K5\bar c_\tau u^M_\tau
-\lambda_t(S^M_{K+1}-S_{\min})
+\mu\sum_{\tau=t}^K a_\tau,
\qquad \lambda_t=\eta_d\operatorname{median}(\bar c_{t:K}).
\]

已付合同费是常数，不再作为可变成本重复收取。uM 是预测补救，不是真实应急 u。S_target 是最近一次合同 LP 的对应时刻目标，µ 的单位为元/kWh。

主 MPC 先取 µ=0，让经济目标决定动作。过渡跟踪方案同一 LP 取 µ=0.01 元/kWh；必要时只在开发期比较 0/0.01/0.05，不把大权重追踪当成经济改进。主目标之后再最小化吞吐；逐槽审计重叠量，不能因原 Q1 的充分条件而省略新执行器检查。

### 7.3 首槽动作与区间内平衡

本轮主回测采用明确的“槽首给出电池动作上限、槽内仅作供需可行性裁剪”近似，不在看到当前真实值后重新做经济优化。只取首槽互斥动作 xM、yM；真实负荷/光伏到来后，按下列确定规则执行：

\[
x^E_t=\min\{x^M_t,\max(g_t+V_t-L_t,0)\},\qquad
y^E_t=\min\{y^M_t,L_t+x^E_t\}.
\]

第一式表示合同与光伏先供负荷，剩余供给不足时缩减充电，绝不靠应急电补充充电；第二式只削减超出实际可吸收量的放电。动作只能缩小，不能借当槽真值增加充放电；互斥动作缩小后，下一状态仍位于原状态与计划下一状态之间，不会越过 SOC 上下界。随后令

\[
b_t=L_t+x^E_t-y^E_t-g_t-V_t.
\]

若 b_t≥0，令 u_t=b_t、w_t=0、vE_t=V_t；若 b_t<0，令 u_t=0、w_t=min(g_t,−b_t)、vE_t=V_t−(−b_t−w_t)。先做上述两项裁剪后，这给出非负可行的弃购优先平衡，且 u_t>0 时必有 xE_t=0。分别记录充电缩减和放电缩减的次数、电量；裁剪后的光伏利用或状态仍超限即报错，不能继续加未定义修补。绝不能把槽内裁剪解释为槽首已知真值。

电池更新使用实际执行/裁剪后的 xE、yE，真实 u 按 5 倍同期价格结算。除供给不足的充电缩减和不可吸收的放电缩减，区间内不新增电池决策。这个抽象不描述 10 min 内的连续细节，需做观测延迟和执行机制敏感性。时序对齐的延迟贪心及所有回退都使用同一两项裁剪，不能通过放宽应急购电用途制造新旧方案差异。

旧贪心基线可在槽内按已观测净缺口改变电池动作，等于额外的“即时反馈”假设。因此同时报告两组：①各自原生控制方式的系统对比，披露时序差别；②双方都只用上一已完成槽决策、使用同一上述平衡/裁剪器的时序对齐对比。新 MPC 的优势必须在②仍有依据，不能依靠比基线多看一个槽。②中的延迟贪心，用上一已完成槽净负荷（首槽用当前中心预测）估计当前盈缺，再按同样功率与 SOC 边界充/放。

### 7.4 回退、验证和采用条件

单次 LP 设 5 s 时间预算（求解器 time_limit，含二阶段的总预算）；超时、非最优、不可行、数值残差或重叠量超限均丢弃该次解，使用时序对齐的延迟贪心首槽动作，后续同样平衡/裁剪。不得对失败日删样。实现记录回退原因、裁剪次数、单次耗时中位数/P95/最大值、每年耗时和求解次数。6 h 规模约每次 250 个变量，年度约 52560 次调用，运行时间应实测，不能只说“小 LP 所以必然快”。

先固定相同合同，隔离比较执行器；再从年初共同状态完整重跑合同—状态闭环，因为执行器改变的 SOC 会改变下一次合同。两种比较都报告费用、应急、弃购、弃光、期末 SOC、规划—执行偏差与 CVaR。是否采用按第 11 节经济规则决定；仅 SOC 更接近不得采纳。

## 8. 问题三：预报信息、调账与单次更新价值

### 8.1 官方点预测与独立残差档案

附件 3 表示发布后 1…24 h 的整点功率预测，不是小时均值。每次 r=0/6/12/18 的零提前期锚点取发布时刻已完成的实际光伏端点；首年 0 时没有前日观测时锚点为 0，之后午夜用前日第 144 点。只使用当前日剩余部分，不假造次日合同。

对日期 d、发布 r、小时提前期 h，定义原始历史误差 e_raw=实际端点−原始预报，只用 j<d 最近 W 个已完成日的同 r/h 原始误差均值修正当次预报。均值为空取 0。校正后非负截断，再连同零锚点线性插值到 10 min。W 使用本轮所选风险窗，基准候选 30 日；旧 60 日版本只作对照。

必须把每个历史日期当时实际发出的“校正后、插值后”10 min 预测另行归档，并由它计算同发布时间的 eV；不能把原始误差减去今天估计的均值，冒充过去校正预测的误差。负荷使用所选 F1/F2 的日初预测；配对 eL−eV 后按第 6 节做净负荷分位。单条原始历史误差会使均值校正等于那一条误差，但校正预测的残差仍来自它当时的版本，不能强制设为 0。

### 8.2 合同与结算

0 时先求 p=q(0)。6/12/18 时读取当前 SOC 和获准的最新预测，冻结已执行时段，仅优化当天剩余合同和规划能量流。每个目标槽最后生效的合同 q 只相对原始 p 结算一次：

\[
C_t=c_t\min(p_t,q_t)+1.5c_t(q_t-p_t)^+
+0.5c_t(p_t-q_t)^++5c_tu_t.
\]

优化时以 q=p+δ+−δ−、δ±≥0 表示，合同项等价于 cp+1.5cδ+−0.5cδ−。因两项共同增加会提高目标，最优解无需同时非零，但仍应检查。各次优化目标只是当次剩余合同的估价，不能把多次目标求和当费用。执行层固定当前合同，不再收一次合同费；真实应急只在最终流水里记。

“原计划全额付费后再追加违约费”的另一解释仅作附录敏感性。Q2 到 Q3 同时改变信息源和合同调整权，费用差不能直接全部归因于信息本身。

### 8.3 8 组信息消融

0 时版本总可用，新增获准集合 S 是 {6,12,18} 的 8 个子集。所有组均在 0/6/12/18 有相同合同重算机会、相同已完成观测反馈和相同执行器；到某一时刻未获准新预报时，只能继续用最近获准版本，不能读取被屏蔽版本或它的校准误差。每个 S 的历史预测档案也要重放相同屏蔽规则。

空集表示“只用 0 时预报但仍有共同重算机会”，不是“只在 0 时优化”。后者另作调控机会消融。每组从相同年初状态连续运行，分别报告费用、应急、月度配对差和每个版本在不同组合中的边际收益。

### 8.4 合成短临更新不等于新官方信息

每 2 h 更新作为次级实验：在最近获准官方预测上，取最近最多 18 个已完成 10 min 槽的光伏实测−基准预测残差均值 m（为空取 0），对未来提前期 ℓ 小时加 m exp(−ℓ/3)，非负截断。该合成预测的历史残差也须按当时的信息重新归档。它既没有凭空增加供应商预报，也没有额外实际未来值。

设旧策略现金费 C_old、新策略费 C_new，新增加的合同更新事件数为 Δn（不计双方共同的每 10 min 电池控制），则

\[
\kappa^*=(C_{old}-C_{new})/\Delta n,\quad
\text{更新净收益}=C_{old}-C_{new}-\kappa\Delta n.
\]

只有 Δn>0 且收益为正才有正的单次成本上限。若实现按事件决定是否更新，触发依据必须是当时预测的收益，不能用事后真收益触发。评审中的 12.06%、6.48%、3.87% 和 235.17 元/次属于旧版本；新主线必须重算，表头写清分母。

## 9. 问题四：预测电价和实际结算价分离

附件 4 只给实际电价，不能假定年初或每天 0 时知道当天全价。Q4-2 复用 Q2 的合同限制，Q4-3 复用 Q3 的合同调整；物理、风险、执行和结算不另起体系。

价格候选为：昨日同槽、最近 7 日同槽均值、最近 30 日同槽均值、最近 30 日同槽中位数。历史不足则用可用历史，空历史用附件 1。分别报告 MAE/RMSE、尖峰时段误差、应急费和现金总费，不先宣称中位数一定最好。

当前代码事实：0 时基准是 30 日同槽中位数，日内修正是最近至多 18 个已完成槽“实际价−0时基准价”的算术均值 m_c，不是中位数。未来修正为 m_c exp(−ℓ/3)，最终下限 0.0001 元/kWh。Q4 每个控制时刻可用已完成价格；合同只能在其获准事件更新。编程实现须让各价格基线采用同一修正规则，初始空集修正为 0。

所有合同 LP、执行 LP 和末端价值只用当前预测价；合同、调整、应急最终按同期真实价结算。现有论文把日内均值写成中位数的地方列为下阶段必改项，本次不改论文或代码。

附加“0 时知道全天真实价格”的理想信息基准，其余预测、执行自由度和结算不变。它是价格信息实验，不自动成为全问题全局最优下界；也不能用该结果选择正式价格预测器。

## 10. 完整历史误差路径压力测试与 CVaR

### 10.1 本轮先做可因果回放的验证

本轮采纳“完整路径验证”，暂缓新增独立 SAA 决策器。每天只取此前已完成日期的配对 eL/eV 全日路径，主回放窗 B=30、附加 B=60，样本不足用已有路径，空库只作启动检查不报告有效尾部风险。相同日期样本对各候选配对使用，不能分别挑好天气样本。

给定当前中心预测，生成 L_s=max(0,Lhat+eL_s)、V_s=max(0,Vhat+eV_s)。先保存同一天中全部 144 槽的联合路径，不逐槽独立抽样；记录非负裁剪比例，说明裁剪可能改变残差分布。等权枚举这 B 条路径属于经验场景回放，不必称蒙特卡洛。对不同预测器，使用各自同日期的残差，披露场景生成也随预测器改变；固定残差场景仅用于隔离执行器比较。

压力测试首先限定在 Q2：从同一日初 SOC 出发，冻结各方法当时制定的合同；执行器只能按时间顺序看到场景中已完成部分，预测与校准不得偷看整条未来路径。每个场景开始时恢复相同初值，场景之间不传 SOC；这不是年度连续回测。年度真实闭环另行评价，不能用样本内场景平均成本代替。

输出均值、VaR、CVaR、最坏日、应急和弃购。场景费用是策略回放后的真实规则现金费，不含终端奖励。Q3/Q4 若扩展压力测试，还须构造相容的预报版本/价格信息路径；本轮不声称只靠 Q2 的 eL/eV 就完成这两问全部信息风险验证。

### 10.2 尾部指标的精确定义

日费用样本 C1…Cn（可来自年度真实日，或来自当前压力测试日，二者分开标注），β=0.90/0.95。VaR 取等权经验左逆分位；CVaR 定义为

\[
\operatorname{CVaR}_{\beta}(C)=\min_{\zeta}
\left[\zeta+\frac{1}{(1-\beta)n}\sum_{i=1}^n(C_i-\zeta)^+\right].
\]

它对尾部分界的样本使用必要的分数权重，不能简单把所有 C≥VaR 的样本平均，尤其有并列值或 n 很小时。Pinball 定义为 ρ_α(r)=r[α−1(r<0)]，评价时 r=N−Ntilde；中心预测 MAE 用 N−Nhat，不把二者混用。β 是费用尾部水平，与风险轨迹 α 不同。

### 10.3 对 SAA 的保留接口与限制

若未来专门开展 SAA，可取共享 p、每条情景分别设置 x_s/y_s/S_s/u_s/w_s，目标为合同费+平均 5 倍应急费−平均终端价值，并满足同样物理约束。全日两阶段补救知道完整情景，通常过于乐观；“第一阶段合同共享”并不等于整个策略严格因果。

其 p 必须在同一因果执行器、相同初始状态和真实后置日期重新评价；不能拿其内层目标与主线的现金回测比较。若加非前视场景树则已经是更重的多阶段模型。本轮不创建该独立优化器，不声称完成 SAA 对照，不因为模型数量限制而偷换名称。

同理，CVaR 本轮是指标，没有放进合同目标，不称“均值—CVaR 优化”。DRO、模糊集半径选择和深度学习暂缓；GA 也非必要，小网格已足够，任何辅助搜索都不能证明全局最优。

## 11. 验证、选型与程序交接合同

### 11.1 时间划分和采用规则

1 月连续预运行；2–7 月开发候选与参数范围；8–9 月选择配置；**2025-09-30 后冻结模型类别、W、α、H、µ及评价规则**。10–12 月只作时间后置最终评价，不根据它反调。冻结的是策略规则，滚动窗口仍可按照已冻结规则使用新近已完成观测。

必须纠正评审最后一句“最终参数由10—12月封存结果确定”：这与其前文协议冲突，不采用。每日无未来数据只说明决策因果，不消除全年挑模型的选择偏差。团队已浏览全年部分结果，10–12 月不能再称完全未见的外部测试；新年度数据才可做真正外部验证。

所有候选从 1 月 1 日同一 6000 kWh 初值开始，之后各自连续传状态。首选配置按 8–9 月累计现金费最小选，若相对差≤0.1% 则选结构更简单/耗时更少者，同时披露不同期末 SOC。尾部风险改善但现金更贵的方案单列“风险型备选”，写明每少 1 kWh 应急或每单位 CVaR 改善的额外费用，不用任意加权分掩盖取舍。期末状态可能影响费用排序，必须同时报告现金、期末 SOC 与统一残值修正参考指标，后者不能替代现金。

### 11.2 先隔离，再联合

按以下顺序，前一环节通过最小运行检查再扩大计算：

1. 固定旧风险规则 W=60、α=0.8、贪心和结算，只替换 F0/F1/F2，分别重建自身残差；先核对点误差，再比较费用。
2. 固定候选预测和执行器，只替换风险构造（旧低光伏分位重构/新净负荷残差）、W 和 α，避免把构造与窗口收益混为一谈。主小网格 W=21/30/45、α=0.8/0.9/0.95。
3. 固定合同隔离贪心/跟踪/经济型滚动 LP，并做第 7 节时序对齐；之后完整闭环重跑。
4. 冻结开发/选择期规则后验证第一组合 F1+30日+0.9+H36/µ0，和实际选中的组合；若不是同一个，明确解释。做完整路径压力测试和真实日 CVaR。
5. 把同一最终预测、风险和执行器传到 Q3/Q4，重新计算所有百分比和模板数据。

正文只集中展示五组：Q1与无储能；三类点预测；风险窗口/分位；贪心与滚动执行；完整路径与尾部验证。Q3 信息价值和 Q4 价格基线放对应问的必要结果表。已有 54 组实验不删除，其余敏感性放附录。容量/效率/更新频率保留重点解释，不为凑实验数扩大模型。

### 11.3 实现字段、算法与停止条件

| 层 | 输入→输出 | 下一阶段现有入口/拟改责任 | 验收与停止 |
|---|---|---|---|
| 数据 | 原始功率、日期、版本→统一单位数组 | microgrid_core.py 的 load_inputs；不修改附件 | 非连续日期、负值/NaN、时刻错位立即停止 |
| 预测 | 截止时间以前数据→Lhat/Vhat/chat和版本档案 | build_analog_forecasts、predict_price；新增可选规则，保留旧基线 | 复算 F1/F2 指标；未来数据扰动不能改变过去预测 |
| 风险 | 同版本已完成残差→安全轨迹和完整路径 | safe_analog_trajectory、safe_update_trajectory | 空库/单样本有定义；不能使用错版本缓存 |
| 合同 | 安全轨迹、SOC、合同权限→p/q、目标SOC | solve_schedule、run_q2_day、run_q3_day | 仅获准时刻调账；LP非最优停止该计划试验并记录 |
| 执行 | 固定合同、可得预测/观测→实际动作、u/w、SOC | causal_dispatch保留；新增第7节滚动LP与裁剪平衡 | 5秒预算、数值检查、回退；不得丢弃失败日 |
| 结算 | 原合同、最终合同、真实价格/应急→逐槽现金 | summarize_day及统一结算函数 | 调整一次结算；流水与汇总一致 |
| 输出 | 各问结果→五份模板副本、论文候选表 | 现有 run_all.py / evaluate_results.py 等需按新合同适配 | 表名/日期/列序不变；本轮不执行新输出 |

依赖为 Python、NumPy、pandas、SciPy/HiGHS、openpyxl；本轮新主线不依赖 scikit-learn（旧基线源码导入它属于实现遗留）。固定实验配置与随机种子 2026，记录代码版本、库版本、求解器状态和运行时间。

本轮尝试导入现有 load_inputs 时因当前 bundled Python 缺少 scipy 失败，退出码1；随后使用 pandas/NumPy/openpyxl 完成只读结构检查和点预测复算，退出码0。没有为了建模文档安装依赖，也没有运行或宣称运行新的年度调度。下一阶段必须先补齐环境并通过最小可运行门禁。

### 11.4 必做数值和因果门禁

- 规划及执行母线平衡最大误差≤1e−6 kWh，SOC/功率/非负边界误差≤1e−7；充放电最大重叠≤1e−5 kWh，检查首末状态和每次动作裁剪。
- 各时槽结算流水与汇总相对误差≤1e−5；模板每日总量等于144列之和，最终调整表写最终合同而非差额。当前最终合同表已正确，不再称输出缺失。
- 检查年初零历史、单残差、夜间全零、重复路径、1月31日到2月1日SOC连续；F1/F2冷启动和价格空历史有唯一结果。
- 至少抽查20天及全部发布边界，扰动决策后实际值/未来预报应不改变当前决策；被屏蔽官方版本和它的校准档案不得影响相应策略。
- 每月、每月低光伏Q20分层报告费用/应急/弃购/弃光/期末SOC，日费用最差20日单列。Q20是事后描述标签，不用于在线决策，不冒充气象标签或因果解释。
- 检查预测安全轨迹的覆盖率和Pinball；分别记录合同目标SOC、执行计划SOC及真实SOC，不把它们混成一个“规划误差”。
- 应急用途边界测试：H=36、µ=0、SOC初值6000，各槽g=L=500 kWh、V=0，首槽价0.0076、其余0.8元/kWh；新增上限应强制所有xM=uM=0，不得因末端估值而买应急电充电。另测“预测有盈余但实际变缺口”：实际充电必须缩到0，再以应急补负荷；检查所有实际槽min(xE,u)≤1e−7 kWh及两种裁剪后的供需/SOC边界。
- 求解非最优、信息泄漏、模板错位或费用不一致时停止相应正式结论。新方案费用与应急均更差且没有尾部或状态解释则不采用，但如实保留失败实验。

M1返工后的作者边界自检：用NumPy在内存构造20万组非负供需、合法SOC与互斥计划动作，按第7.3节两项裁剪和平衡公式验证，最大母线残差4.55e−13 kWh，SOC仍在1200—10800 kWh，应急与充电的最大数值重叠2.28e−13 kWh，退出码0。上述低价反例的两个新增上限均为0；“合同500、实际负荷600、光伏0、原定充电100 kWh”裁为充电0、应急100 kWh。此检查只验证边界代数，不等于完整MPC求解或年度回测。

### 11.5 关键敏感性与假设失效

效率0.9/0.9与sqrt(0.9)/sqrt(0.9)、时间标签两种映射、终端影子价值0/0.5/1/1.5倍、容量与功率±20%、初始SOC20%/50%/80%及两种合同结算均放附录。初始SOC的20/80%仅用于年度敏感性，Q1正式初末6000不变；0.5倍下调费是题意约束，不当作寻优参数。

若真实存在并网限额、负电价/售电、未来官方预报发布时间不同或额外信息延迟，则相应物理/信息假设失效，不能只调α继续用旧结论。末端残值不能证明无限时域最优；边际分位不能提供整日联合风险保证；历史路径数量很小的CVaR估计需注明不稳定。

### 11.6 硬输出映射

Q1填result1.xlsx；Q2、Q3、Q4-2、Q4-3分别填result2.xlsx、result3.xlsx、result4-2.xlsx、result4-3.xlsx，后四份正式日期均为2025-02-01—12-31共334天，1月仅用于连续预运行。原模板表名和列序不变。

论文表1取10:00–10:10、12:00–12:10、14:00–14:10、16:00–16:10、18:00–18:10、20:00–20:10六槽，按自然区间的一基索引分别是61、73、85、97、109、121，并给出全天总购电量和现金费。表2取0–4、4–8、8–12、12–16、16–20、20–24时六段，每段汇总连续24槽的交流侧充/放电量，另取S1和S145。Q1用计划结果；Q2—Q4用真实执行充放电和SOC，购电量区分日初合同p、最终合同q和应急u，不混成一个合同量。

Q2—Q4论文重点日期为2025-03-20、06-21、09-23、12-21，逐日给出表1、表2和紧急购电表3；Q3/Q4-3另并列原合同与最终合同。所有指定槽均按第2.3节自然索引抽取，不受模板右移表头影响。

应急底账保留每个10 min槽；正式工作簿及表3按原题表4示例，将同一自然日内连续u>1e−8 kWh的槽合并成最大连续区间，购电量为区间逐槽u之和；不跨零应急槽，不跨午夜。全年无应急时标“无”、电量0。小于阈值的正量单列数值尾差，验证合并区间总量加尾差等于逐槽总量；不得丢弃其现金结算。现有run_all.py仍逐槽输出，该合并属于下一阶段明确改项，本轮不改工作簿。

## 12. 本轮结论、证据和文献

### 12.1 能说什么，不能说什么

能说：短周期预测在本附件的净负荷误差上明显优于评审记录的旧模型；新设计已把预测、风险、合同、执行、结算和评价条件写成可交接合同。不能说：F1+30日+0.9+MPC已经达到某个年度费用、已经完成SAA或已修改论文。已核查代码常量 DEFAULT_ALPHA=0.80、DEFAULT_RISK_MODE=unclustered；本轮只是新设计，不能把它们说成已经改成0.9。

论文可以突出“数据与调度成本驱动的因果选型”“预测与安全量分层”“规划—执行—结算和信息价值”，但这是应用建模亮点，不宣称报童分位或MPC本身是新理论。离线冻结选型不称自适应算法；只试了更多模型不构成创新。

### 12.2 输入快照（不计算哈希）

项目根目录为 C:/Users/lenovo/Desktop/Mathmodel/2026MCM/2026MCM-1.1-Sol。下列数据文件修改时间均为2026-09-10 18:58:12：

| 根目录相对路径 | 字节数 |
|---|---:|
| data/C题.pdf | 354389 |
| data/附件/附件1.xlsx | 16003 |
| data/附件/附件2.xlsx | 907128 |
| data/附件/附件3.xlsx | 293526 |
| data/附件/附件4.xlsx | 409790 |

评审输入 reviews/模型对比评审意见.md 为19387字节，修改于2026-09-11 17:39:27；用户粘贴输入 C:/Users/lenovo/.codex/attachments/0f2ccfd8-2f23-4693-b3af-1a1a2f4d358f/pasted-text.txt 为27566字节，修改于2026-09-11 17:43:53。只读，不覆盖用户评审改动和 Checkpoint-V1。

### 12.3 方法出处及其用途

1. Cominesi等，A Two-Layer Stochastic Model Predictive Control Scheme for Microgrids，IEEE Transactions on Control Systems Technology，2018，DOI：[10.1109/TCST.2017.2657606](https://doi.org/10.1109/TCST.2017.2657606)。作为快慢两层控制的背景，不声称本设计等同于该文随机控制器。
2. Hu等，Model predictive control of microgrids – An overview，Renewable and Sustainable Energy Reviews，2021，DOI：[10.1016/j.rser.2020.110422](https://doi.org/10.1016/j.rser.2020.110422)。用于滚动控制的适用范围和边界。
3. Alonso等，Optimization Models under Uncertainty in Distributed Generation Systems: A Review，Energies，2022，15(5):1932，DOI：[10.3390/en15051932](https://doi.org/10.3390/en15051932)。本轮双引擎检索返回该条目，sources为OpenAlex和AnySearch，cross_validated=true；仅作为不确定性方法背景，未据其推断本题效果。

本轮使用双引擎检索脚本 hybrid_scholar.py，查询“microgrid battery rolling horizon model predictive control quantile forecast residual scenario CVaR”，limit=8，退出码0，得到OpenAlex与AnySearch交叉匹配结果。前两条沿用既有核验文献，本轮没有重新取得出版商全文；第三条为双源元数据交叉核对。额外DOI直连校验因当前网络认证/审批环境未完成，不冒称已重新取得出版商页面。

评审中的“695、孟晓丽2016、2506、s40866”等缺少足够题名/作者/DOI，本轮不补造引用。用户粘贴内容所提外部同题PDF未作为本轮附件提供，其约1400万元费用和参数只当外部转述，不能拿来当我方结果。

国赛规则依据沿用项目的 paper/OFFICIAL_2026_RULES_CHECKLIST.md、docs/2026国赛校内安排.md，以及官方[竞赛规则](https://www.mcm.edu.cn/html_cn/node/9d8e511fe7a1447b35f53a82c908e2e0.html)、[论文格式](https://www.mcm.edu.cn/html_cn/node/4cd596519c9eb9fbd866398f6df0caa3.html)、[AI使用规则](https://www.mcm.edu.cn/html_cn/node/fef94648f2836ab6cc81586f4c38512b.html)。本轮不处理论文编译或提交；后续论文须人工核验、按规则披露AI使用。
