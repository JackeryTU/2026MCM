# 2026 年全国大学生数学建模竞赛 C 题分析报告

> 文档性质：建模与复现草案。参赛队必须结合原题、官方规则和程序输出逐项核验、人工改写并承担最终提交责任。

## 1. 任务重述与总目标

题目要求在 10 min 时间粒度下，为含光伏和电化学储能的小区微网制定外部电网购电计划。四问逐步引入三类不确定性：实际负荷与实际光伏偏离事前估计、日内光伏预测更新、逐日逐时电价变化。核心矛盾是：少买会触发 5 倍电价的应急购电，多买又可能形成无效购电或付出向下调整违约金；储能可在时段之间搬移能量，但受容量、功率和效率限制。

总目标是在任何时段均满足负荷、储能物理约束和信息可得性约束的前提下，最小化计划购电费、日内调整费和应急购电费，并输出题目指定的 5 份结果工作簿（result1、result2、result3、result4-2、result4-3）、典型日表格、费用分解和更新时刻建议。

## 2. 输入、数据审计与时间口径

### 2.1 文件与数据规模

| 文件 | 主要内容 | 规模 | 用途 |
|---|---:|---:|---|
| 附件 1 | 典型日电价、负荷、光伏预测功率 | 144 个 10 min 时段 | 问题一；问题二的固定日内电价 |
| 附件 2 | 2025 年实际负荷、实际光伏功率 | 365×144，各 2 张表 | 问题二至四回测真值 |
| 附件 3 | 每日 0/6/12/18 时发布的未来 24 h 光伏预测 | 365×4×24 | 问题三、四滚动更新 |
| 附件 4 | 2025 年 10 min 电价 | 365×144 | 问题四 |
| 附件 5 | result1/2/3/4-2/4-3 模板 | 固定表名和表头 | 最终结果写入 |

原始文件保持只读，输出使用模板副本。

### 2.2 审计结果

1. 附件 1 有 144 个唯一时刻；电价、负荷和光伏均无缺失、无负值。电价范围为 0.3713–1.3952 元/kWh，日负荷电量为 111024.8081 kWh，日光伏预测电量为 55482.8357 kWh。
2. 附件 2 的日期连续覆盖 2025-01-01 至 2025-12-31，两个工作表均为 365×144；无缺失、无负值。负荷功率范围为 1995.7176–7978.8849 kW，光伏功率范围为 0–10216.2 kW。
3. 附件 3 每天恰有 0:00、6:00、12:00、18:00 四次预测，每次 24 个小时值；无缺失、无负值。日期单元格按原表向下填充，不把空白日期当作缺测。
4. 附件 4 的日期、144 个时刻与附件 2 完全对齐；电价范围为 0.0076–1.7936 元/kWh。
5. 附件 1 的负荷、光伏、电价曲线分别近似等于附件 2、2、4 的逐时年度均值，最大逐点差分别约为 `5.04e-5 kW`、`0.0431 kW`、`5.15e-5 元/kWh`，因此可把附件 1 解释为“年度典型均值日”。
6. 附件 3 的历史预测误差随提前期增加而增大：1 h、6 h、12 h、18 h、24 h 提前期的光伏 MAE 约为 18.69、89.93、159.71、224.32、374.79 kW。这为日内滚动更新提供了直接数据依据。

极端高负荷和高光伏值在相邻时段连续出现，且均满足非负性与物理量级，不作为异常值删除。后续仅对预测值做非负截断，不修改真实值。

### 2.3 10 min 时段映射

输入表的 144 个标签从 `00:10` 至次日 `00:00`，可自然解释为每个 10 min 时段的结束时刻。因此计算时令第 \(t\) 个数对应自然日区间 \([(t-1)10,t10)\) min：第 1 行是 0:00–0:10，第 144 行是 23:50–24:00。全部能量由

\[
\Delta t=\frac{1}{6}\ \mathrm{h},\qquad E=P\Delta t
\]

换算。题目给出的结果模板却把第 1 个输出列写成 `0:10-0:20`，比原始数据的自然区间右移 10 min。程序不改动官方模板表头，按原始数据行序逐列填入，并在论文中明确该表头不一致；所有 SOC、0–4 h 聚合和指定时刻抽取均以自然日计算索引为准。另以“模板表头作为开始时刻”的整体循环平移映射重算典型日，作为边界敏感性检验。

## 3. 模型假设

1. 外部电网购电和应急购电均无额外功率上限；若实际存在并网容量上限，应把该上限加入约束后重算。
2. 光伏可弃光且无直接罚金，外购电不可反向售电。已经支付但未消纳的计划购电记为无补偿弃购，不抵扣费用。
3. 题目给出的 90% 储能效率同时用于充电和放电，即 \(\eta_c=\eta_d=0.9\)。另以“90% 为往返效率，即 \(\eta_c=\eta_d=\sqrt{0.9}\)”进行敏感性检验。
4. 充、放电功率上限均为 5000 kW；单个 10 min 时段的能量上限为 \(833.3333\) kWh。\(x\) 表示电池交流侧吸收量、\(y\) 表示电池交流侧送出量，两者都受该上限约束；SOC 分别增加 \(\eta_cx\) 和减少 \(y/\eta_d\)。电量边界为 1200–10800 kWh，额定容量为 12000 kWh。
5. 问题一按题意取 \(S_1=S_{145}=6000\) kWh。问题二至四从 2025-01-01 0:00 的 6000 kWh 开始逐时连续传递 SOC，不强制每日首末相等。每次有限时域优化使用终端储能价值防止末端透支，12 月 31 日末仍按同一价值计量并单列期末 SOC。
6. 同一时段不会有经济上有意义的同时充放电。计划 LP 先最小化经济费用，再在最优费用容差内最小化总充放电量，以消除退化同时充放电解；真实运行采用单向因果反馈规则，结构上不会同时充放电。
7. 在 0:00 制定计划时，只使用该日以前的实测数据和题目事先提供的典型日资料、当天已发布的光伏预测；在 6/12/18 时调整时，只使用当时已经发布的预测和已经实现的观测，禁止使用未来实际值。附件 1 虽与全年度均值接近，但作为题设外生先验使用，不能称其为由纯历史训练得到的量。问题四另按第 8 节预测电价。

假设分级：无外网购电上限、允许无补偿弃购/弃光、上述信息集为三项关键边界；若改变，必须重构可行域或信息集并重新回测。效率解释、初末状态与终端价值、吞吐次级优化是可检验的实现设定，分别用效率替代、末态披露与物理残差检验复核。

## 4. 符号与储能—功率平衡模型

设一天含 \(T=144\) 个时段，历史误差情景集合为 \(\Omega\)。所有购电、充放电变量均按每时段 kWh 计。

- \(L_t,V_t\)：负荷与可用光伏电量；
- \(p_t\)：0:00 确定的计划购电量；
- \(q_t^{(r)}\)：第 \(r\) 次更新后的合同购电量；
- \(c_t\)：电价；
- \(x_t,y_t\)：充电、放电量；
- \(S_t\)：时段开始时储能电量；
- \(u_t\)：应急购电量；
- \(v_t\)：实际利用的光伏量；
- \(w_t\)：已付费但未消纳的合同电量。

通用物理约束为

\[
g_t-w_t+v_t+y_t+u_t=L_t+x_t,
\]

\[
S_{t+1}=S_t+\eta_c x_t-\frac{y_t}{\eta_d},
\]

\[
1200\le S_t\le10800,\quad 0\le x_t,y_t\le833.3333,
\]

\[
0\le v_t\le V_t,\quad 0\le w_t\le g_t,\quad u_t\ge0.
\]

其中问题一、二令 \(g=p\)，问题三、四令 \(g\) 为当前有效的 \(q^{(r)}\)。供应充足由平衡式和 \(u\ge0\) 保证；问题一令 \(u=0\)。问题一有日电量循环约束 \(S_{T+1}=S_1\)，问题二至四则令下一时段/下一日初值等于上一时段/上一日末值。

## 5. 问题一：确定性线性规划

附件 1 给定完整的典型日电价、负荷和光伏预测，因此采用确定性线性规划：

\[
\min \sum_{t=1}^{T} c_t p_t.
\]

约束为第 4 节通用模型，并令 \(u_t=0\)、\(g_t=p_t\)、\(w_t=0\)、\(S_1=S_{T+1}=6000\)。若光伏超过负荷与储能可吸收量，通过 \(v_t<V_t\) 表示弃光。由于模型是连续线性规划，可用 HiGHS 获得全局最优解。随后在最优费用增加不超过 \(10^{-8}\max(1,|C^*|)\) 的条件下最小化 \(\sum_t(x_t+y_t)\)，减少无效循环；该次级目标不保证调度唯一，仍须逐时检查同时充放电量。

输出：144 点计划购电量，4 h 聚合充放电量与 0/24 时储能电量，总购电量和总费用，并填充 `result1.xlsx`。

## 6. 问题二：因果情景预测与分位数安全计划

### 6.1 严格因果预测

对每个待预测日 \(d\)，只使用 \(j<d\) 的历史数据。负荷和光伏分别采用“指数衰减相似日”点预测：

\[
\hat z_{d,t}=\frac{\sum_{j<d}a_{dj}\exp[-(d-j)/60]z_{j,t}}
{\sum_{j<d}a_{dj}\exp[-(d-j)/60]},
\]

其中负荷在同星期类型时 \(a_{dj}=2\)，否则为 1；光伏取 \(a_{dj}=1\)。仅保留最近 90 d。记该窗内历史日数为 \(n_d\)，上式非空历史加权均值为 \(m_{d,t}\)，附件 1 的典型日为 \(z_t^0\)。实际点预测唯一取 \(\hat z_{d,t}=\max\{0,\rho_d m_{d,t}+(1-\rho_d)z_t^0\}\)，其中 \(\rho_d=\min(1,n_d/21)\)。若 \(n_d=0\)，不计算空集均值，直接令 \(\hat z_{d,t}=z_t^0\)。收缩的 21 d 是事前固定的三周热启动设定，不用全年测试误差调节。该模型不需要天气外生变量，符合题目提供的信息集，并能在 1 月作为 2 月回测的热启动。

对每个历史日，按当时可用历史产生一步前推预测，得到整日联合残差轨迹

\[
r_j=(L_j-\hat L_j,\;V_j-\hat V_j).
\]

在当前日前最近 60 条可用残差中，对标准化后的 288 维轨迹执行确定种子的 K-means，取至多 9 个簇中心作为负荷—光伏联合情景，簇频率为 \(\pi_\omega\)。确定性边界规则如下：空残差库用一条全零负荷—光伏残差、概率 1 作为退化情景，不声称它已估计出首日不确定性；单条残差直接作为唯一中心、概率 1。其余情况按各坐标总体标准差（分母为样本数）缩放，标准差小于 \(10^{-9}\) kW 的坐标将缩放分母置为 1 kW。簇数取配置上限、样本数及不同标准化轨迹数三者的最小值，随机种子 2026，初始化次数 10；只保留非空簇，中心为簇内原单位残差算术均值，概率为簇样本数除以总样本数。由此夜间全零与重复轨迹不产生空簇均值或 NaN。构造

\[
L_{t\omega}=\max(0,\hat L_t+r^L_{t\omega}),\quad
V_{t\omega}=\max(0,\hat V_t+r^V_{t\omega}).
\]

整条日轨迹聚类而非逐时独立抽样，在情景构造层保留时序相关性和负荷—光伏相关性；调度 LP 只接收逐时分位数拼接后的单轨迹，并未完整利用跨时联合分布，不能称为多阶段随机最优控制。情景只用于构造分位数安全轨迹和离线评价，不允许优化器在运行前知道“实际属于哪个完整日情景”。

### 6.2 场景校准的单阶段线性计划

加权经验分位数采用左连续逆累积分布的确定定义：对单时段情景值升序排列为 \(z_{(i)}\)，合并或保留同值均不影响结果，归一化权重累计为 \(F_i\)，取 \(Q_\alpha=z_{(\min\{i:F_i\ge\alpha\})}\)，不在相邻情景值之间线性插值。唯一情景的所有分位数均等于该情景值；故首日安全轨迹退化为给定典型日点预测。首日之后只能在整日实测完成后把其真实前推残差加入次日残差库。

应急购电是短缺损失，未消纳计划电量是过量损失。在忽略储能耦合的一维报童近似下，临界分位数为

\[
\alpha^*=1-\frac{c}{5c}=0.8.
\]

这是因为多购 1 kWh 的边际成本为 \(c\)，少购后应急的边际成本为 \(5c\)，最优条件是 \(\Pr(N>p)=c/(5c)=0.2\)。模型只采用一个联合定义：先对每个联合情景计算 \(N_{t\omega}=L_{t\omega}-V_{t\omega}\)，再令 \(\tilde N_t=Q_{0.8,\omega}(N_{t\omega})\)。为在 LP 中保留弃光量，取

\[
\tilde V_t=\max\{Q_{0.2,\omega}(V_{t\omega}),-\tilde N_t,0\},\qquad
\tilde L_t=\tilde V_t+\tilde N_t,
\]

从而 \(\tilde L_t,\tilde V_t\ge0\) 且严格满足 \(\tilde L_t-\tilde V_t=\tilde N_t\)。不再使用“分别取负荷高分位和光伏低分位后相减”的另一套公式。以该唯一保守轨迹求单阶段 LP：

\[
\min_{p,x,y,S,v}\sum_{t=1}^{T}c_tp_t-\lambda_d(S_{T+1}-S_{\min}),
\]

满足第 4 节物理约束，令 \(u=w=0\)，初始 SOC 为当天真实初值。终端价值

\[
\lambda_d=\eta_d\,\operatorname{median}(c^{\mathrm{known}}_{d,1:T})
\]

是期末电量替代后续外购电的启发式影子价格；固定价使用剩余时域典型日电价中位数，变价使用第 8 节剩余时域预测电价中位数，并不读取次日真值。该软价值允许 SOC 跨日变化，但不能保证无限时域最优。残值项不计入题目要求的实际现金购电费用；报告现金费用及期末 SOC，不能把含残值的求解目标当作现金费用。

计划 \(p\) 制定后，真实运行不再解带完整日真值的补救 LP，而是每 10 min 仅基于当前已观测的 \(L_t,V_t,S_t\) 执行因果反馈：若 \(p_t+V_t-L_t\ge0\)，先在功率和容量允许范围内充电，再优先把不能消纳的合同量记为 \(w_t\)，最后弃光；若为负，先在功率和 SOC 下界允许范围内放电，再把剩余缺口记为 \(u_t\)。费用按全天计划 \(p_t\) 支付，未消纳计划量不退款。该控制不读取任何未来实际量，并把实际 \(S_{t+1}\) 连续传给下一时段和下一日。

### 6.3 评价

预测层报告 MAE、RMSE、MAPE（负荷真值大于 1 kW、光伏真值大于 100 kW 的时段）和历史原始残差构成的 90% 经验区间覆盖率；它不是有限样本覆盖保证。决策层报告 2–12 月费用、应急电量及次数、弃购/弃光、储能吞吐、SOC 越界数。与三个基线比较：附件 1 固定典型日计划、未经分位数修正的点预测计划、无储能计划。主参数固定为事前确定的 0.8；另比较 \(\alpha\in\{0.70,0.75,0.80,0.85,0.90,0.95\}\) 的回测敏感性，不根据本次全年度测试费用重新选择并追溯修改主参数。

## 7. 问题三：日内预测更新与滚动 MPC

### 7.1 光伏预测处理

附件 3 的列名为“预报 1 小时”至“预报 24 小时”，题面又明确其为未来整点功率预报，故将其解释为发布后第 1–24 个整点的**点预测**，而非小时平均功率。历史误差以相应整点的 10 min 实测端点值相减；在 10 min 调度网格上，以发布时刻已经观测到的光伏功率为零提前期锚点，与后续整点预测作分段线性插值。该处理既不使用未来实测值，也避免把整点值错误地重复成整小时平台。对发布时刻 \(r\in\{0,6,12,18\}\) 和提前期 \(h\)，只利用此前最近 60 个日期已实现误差的算术均值做偏差校正：

\[
\tilde V_{d,r,h}=\max\{0,\hat V^{\mathrm{raw}}_{d,r,h}+\bar e_{r,h}^{(<d)}\}.
\]

仅使用当次发布至当天 24:00 的预测段，即提前期 1 至 \(24-r\) h；此前各日期这些误差在当前决策日之前均已实现。把该段中心化整点误差从零锚点插值到 10 min 网格，与同期历史负荷残差联合聚为至多 9 个情景，形成 0.8 分位数安全轨迹。未使用的跨日预测仅用于描述预测误差随提前期变化，不参与当前调度或终端价值。负荷预测继续使用问题二的因果相似日方法。情景只用于当时可得分布的分位数校准，不产生可预见未来的情景补救决策。

冷启动约定：首日各次发布没有前日同发布/提前期误差时，偏差均值置 0，中心化误差与负荷误差采用第 6.1 节的单条零残差退化情景。即便首日 6 时已有当日实测，也不把未完成当日混入按历史日期构造的残差库；当前观测只用于锚点和明确规定的临近预测。首日 0:00 无前日端点观测，光伏零提前期锚点固定为 0 kW，作为午夜无光伏的外生启动设定；之后每个零点取前日第 144 个端点，日内发布取当前已实现的发布时刻端点，绝不取下一时段实测。仅有一条历史误差时，均值等于该误差、其中心化光伏误差为零，负荷残差仍保留其真实历史值；聚类与分位数沿用第 6.1–6.2 节的退化、零方差及概率规则。额外时刻的临近修正若可用观测集合为空，修正均值也置 0。

### 7.2 更新与结算

在 0:00 先生成全天计划 \(p=q^{(0)}\)。在 6:00、12:00、18:00，读取最新预测、当前真实 SOC 和已执行量，仅重算尚未执行的时段；过去决策冻结。令

\[
\delta^+_t=\max(q_t-p_t,0),\qquad
\delta^-_t=\max(p_t-q_t,0).
\]

每个尚未执行时段可被多次重算，但只有执行前最新一版 \(q_t\) 生效，历史中间报价被覆盖；偏差只相对 0:00 原计划 \(p_t\) 结算一次，不把同一时段的多次重报重复收费。主口径采用有经济意义的交割结算：下调时，实际交割 \(q_t\) 按基础价购买，取消的差额支付 50% 违约金；上调时，原计划部分按基础价购买，增加部分按 1.5 倍电价购买。故

\[
C=\sum_t\left[c_t\min(p_t,q_t)+1.5c_t(q_t-p_t)_+
+0.5c_t(p_t-q_t)_++5c_tu_t\right].
\]

每次更新以最新分位数轨迹为物理平衡输入，在线性化的上、下调差额变量下，最小化上述交割费用减终端储能价值；只执行到下一更新的合同量与因果反馈动作，构成滚动 MPC。模板仅记录当前自然日的初始计划和最终有效购电量。若实际仍低于预测，则以应急电量 \(u_t\) 补足；若最终合同仍过量，按已结算合同记录未消纳量 \(w_t\)，不退款。

题意也可能被解释为“初始计划费始终全额支付，再额外收取偏差费”。为避免口径决定结论，程序用下式重算敏感性：

\[
C'=\sum_tc_tp_t+\sum_t1.5c_t(q_t-p_t)_+
+\sum_t0.5c_t(p_t-q_t)_++\sum_t5c_tu_t.
\]

正文采用前一口径，附录对同一组已执行策略复算两种结算费用与更新时刻排序；后一口径仅为会计敏感性，并非该规则下重新优化的最优策略。后一口径下，向下调整在存在无退款弃购时可能被严格支配，这一点必须在解释中披露。

### 7.3 是否需要增加预测时刻

候选调整时刻为每 2 h 一次，并与仅零点、0/12、0/6/12/18 的消融比较。附件只含 0/6/12/18 时真实发布数据，因此无法直接证明额外供应商预报的价值。中间时刻采用可复现的合成因果临近预测：以最新已发布且偏差校正的光伏点预测为基准，在最近发布后至候选时刻的已实现区间中，取至多 18 个 10 min 点的实测减预测均值 \(b\)，未来提前 \(\tau\) 小时加上 \(b e^{-\tau/3}\)。将相同修正作用于该次原安全光伏轨迹并非负截断，保留原安全负荷轨迹；不声称这是新获得的真实预报，也不声称其不确定性已重新校准。

采用逐年严格前推回测，对每个候选更新集合计算：

\[
\mathrm{NetBenefit}(H)=C_{\{0,6,12,18\}}-C_H-\kappa N_H,
\]

其中 \(N_H\) 是额外更新次数，\(\kappa\) 是一次重新报价的管理成本。主结果报告 \(\kappa=0\)，并给出使每个新增时刻刚好不值得采用的盈亏平衡 \(\kappa^*\)。只有当年度费用下降、四个季节代表日均不恶化且提升超过数值容差 0.1% 时，才建议新增时刻。

## 8. 问题四：时变电价下的统一模型

附件 4 仅给出实际电价，没有说明全天价格在零点已知，因此问题四将决策电价与结算电价分离。零点用此前最多 30 d 同时段价格中位数预测当天价格；1 月 1 日无历史时使用附件 1 的典型日电价先验。在日内调整时刻 \(r\)，在零点预测上加上最近至多 18 个已实现 10 min 时段“实际减预测”误差的均值，并按未来提前期以 3 h 时间常数指数衰减，最后截断为至少 0.0001 元/kWh。优化器与终端残值仅使用该预测电价；当期现金结算和 5 倍应急费用使用附件 4 同期实际电价。不得把这一结果称为已知全天价格条件下的全局最优。

在此信息集下复用原物理模型：

- 4-2：复用问题二的分位数安全计划与因果反馈，输出 `result4-2.xlsx`；
- 4-3：复用问题三的滚动 MPC 和结算模型，输出 `result4-3.xlsx`。

时变电价会强化低价充电、高价放电，但应急购电仍是同期价格的 5 倍，故高价时段的预测不足尤其昂贵。比较固定价与变价结果时，使用费用、应急电量、峰谷套利量、弃光率、储能等效循环次数和 SOC 约束活跃率六项指标。

## 9. 算法流程与实现合同

1. 读取并验证附件哈希、日期、时刻和模板结构；统一转换为 kWh。
2. 对问题一组装确定性 LP，用 SciPy/HiGHS 求解并做二阶段吞吐最小化。
3. 从 2025-01-01 的 6000 kWh 初值开始连续模拟；1 月既是 SOC 必需的预运行期，也是 2 月前的预测热启动期。从 2 月 1 日开始写入模板，每天重新拟合严格因果预测、构造分位数轨迹并求问题二计划。
4. 问题三在每个代表日/全年日上按 0/6/12/18 事件顺序循环：预测—优化—执行到下一事件—更新 SOC—再优化。任何未来实际值不得进入当次优化。
5. 问题四替换日电价并重复第 3–4 步。
6. 把完整结果写入附件 5 模板副本，保留表名、列顺序、日期格式和数值格式；执行公式错误、空单元格、维度和全天合计审计。
7. 固定随机种子为 2026，记录 Python、NumPy、SciPy、pandas、openpyxl 和绘图库版本，并生成复现清单。

依赖：Python 3.11+；NumPy、pandas、SciPy（HiGHS）、scikit-learn、openpyxl、matplotlib。若 scikit-learn 不可用，使用 SciPy `cluster.vq.kmeans2` 的确定种子实现，不改变模型定义。

## 10. 验证、敏感性与失败条件

### 10.1 必做验证

- 约束残差：功率平衡最大绝对误差不超过 \(10^{-6}\) kWh；SOC、功率和非负约束违反不超过 \(10^{-7}\)。
- 费用复算：逐时结算流水重算与汇总费用差小于 \(10^{-5}\) 相对误差，每个时段的调整差额只相对 0:00 计划出现一次。
- 因果与非前视审计：随机抽取至少 20 天，确认训练与残差库最大日期严格早于决策日；6/12/18 更新不读取发布时刻之后的观测；真实控制每步仅访问当前及过去观测。
- 预测检验：滚动起点评估 MAE、RMSE、区间覆盖率，不用全年度随机切分。
- 决策基线：与固定典型日、确定性点预测、无储能方案比较；随机模型应显著减少高价应急风险，若总费用更高则给出风险—费用解释。
- 复算一致性：工作簿全天合计等于 144 列之和，典型日论文表与工作簿逐项一致。
- 冷启动边界：测试首日零点（无历史、零光伏锚点）、首日 6 时首次更新（使用已实现锚点但历史偏差仍为零）、单条残差、夜间零方差、全部重复轨迹和加权分位数边界；检查输出全为有限值、概率归一且无未来实测参与。逐槽核对 1 月 31 日末 SOC 与 2 月 1 日初 SOC 完全连续，不在正式模板起始日重置为 6000 kWh。

### 10.2 敏感性参数

| 参数 | 主值 | 检验范围 |
|---|---:|---:|
| 充、放电效率 | 0.9/0.9 | \(\sqrt{0.9}/\sqrt{0.9}\) |
| 情景数 | 9 | 5、12、16 |
| 残差窗 | 60 d | 30、90 d |
| 应急倍率 | 5 | 3、4、6、8 |
| 电池容量 | 12000 kWh | 0.8、1.0、1.2 倍 |
| 充放电功率 | 5000 kW | 0.8、1.0、1.2 倍 |
| 调整结算 | 交割量基础价口径 | 原计划全额支付口径 |

### 10.3 判为失败的条件

出现以下任一情况，模型结论不得进入论文定稿：LP 非最优或不可行；SOC/平衡残差超阈值；年度回测发生未来数据泄漏或违反非前视约束；结果模板存在空白结果、错位日期或全天合计错误；同一时段同时充放电超过 \(10^{-5}\) kWh；逐时结算流水不能复算到汇总费用；滚动更新相对基线在费用与应急电量上均无改善且无可解释的风险收益；关键结论在两种时间映射或两种结算口径下方向反转而未披露。

## 11. 预期创新点与证据链

1. **联合轨迹情景而非逐时独立误差**：在情景构造层保留负荷—光伏和时序相关性，调度层仅使用其逐时分位数压缩轨迹；以区间覆盖率、应急电量及费用对比检验实际价值，不预设完整相关结构已被 LP 利用。
2. **场景校准分位数计划 + 因果反馈**：以 5 倍应急成本导出 0.8 安全分位数，避免让储能补救变量预见完整日情景；以非前视审计和基线回测验证。
3. **单次偏差结算嵌入滚动 MPC**：每个时段只相对 0:00 原计划结算最终差额，把向上加价、向下违约和 5 倍应急统一在线性目标中；用 0/6/12/18 消融和新增时刻盈亏平衡分析验证。
4. **字面歧义显式敏感性**：时间标签、90% 效率、调整结算三处均给出替代口径；以结论稳定性而非单一数值支撑建议。

## 12. 可追溯文献与规则依据

- Raimondi Cominesi et al., “A Two-Layer Stochastic Model Predictive Control Scheme for Microgrids,” *IEEE Transactions on Control Systems Technology*, 2018, DOI: [10.1109/TCST.2017.2657606](https://doi.org/10.1109/TCST.2017.2657606)。用于随机 MPC 与快慢两层滚动控制依据。
- Hu et al., “Model predictive control of microgrids – An overview,” *Renewable and Sustainable Energy Reviews*, 2021, DOI: [10.1016/j.rser.2020.110422](https://doi.org/10.1016/j.rser.2020.110422)。用于微网 MPC 建模综述依据。
- Liang & Zhuang, “Stochastic Modeling and Optimization in a Microgrid: A Survey,” *Energies*, 2014, DOI: [10.3390/en7042027](https://doi.org/10.3390/en7042027)。用于随机规划与不确定性情景依据。
- Zhao et al., “Stochastic Optimization of Microgrid Participating Day-Ahead Market Operation Strategy with Consideration of Energy Storage System and Demand Response,” *Energies*, 2020, DOI: [10.3390/en13051255](https://doi.org/10.3390/en13051255)。用于日前市场两阶段情景调度依据。
- Silvente et al., “A rolling horizon stochastic programming framework for the energy supply and demand management in microgrids,” 2015, DOI: [10.1016/B978-0-444-63576-1.50081-9](https://doi.org/10.1016/B978-0-444-63576-1.50081-9)。用于滚动时域与输入更新依据。
- 2026 年全国大学生数学建模竞赛[竞赛规则](https://www.mcm.edu.cn/html_cn/node/9d8e511fe7a1447b35f53a82c908e2e0.html)、[论文格式规范](https://www.mcm.edu.cn/html_cn/node/4cd596519c9eb9fbd866398f6df0caa3.html)与[人工智能工具使用规则](https://www.mcm.edu.cn/html_cn/node/fef94648f2836ab6cc81586f4c38512b.html)。后续论文必须在参考文献前声明 AI 使用情况，支撑材料中提供完整使用详情，并由团队人工核验和修改。
