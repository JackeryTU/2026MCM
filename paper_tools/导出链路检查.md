# 最小真实导出检查（不是完整论文）

2026-09-11 01:00 左右，北京时间。正式截止时间为 2026-09-13 20:00，北京时间；内部建议冻结/安全导出时间保持 9 月 13 日 18:00/19:00。

- 由内置 cumcm 模板初始化 export_smoke，保留 A4 与四边 2.5 cm 基线。引用真实典型日数据和 result_q1_optimal_schedule.png。
- doctor：退出 0。XeTeX/TeX Live 2025，latexmk 4.86a，Poppler 25.02.0，pypdf 6.13.2。LaTeX-only 不依赖 Pandoc。修复版本探测器，忽略 latexmk 的 Windows 控制台编码前缀。
- build：退出 0，无 warnings/issues，实际 PDF 为 213730 bytes、2 页。
- validate：退出 0，未启用完整质量门槛；正文 1 页、公式 1、图 1、表 1、q1 图覆盖。A4，无空白页，无未嵌入字体，图片最低有效 DPI=333。不能作为 W1/W2 通过证据。
- pdftoppm：退出 0，实际打开 smoke_render/page-1.png 和 page-2.png。中文正常、原生公式和上下标正常、图中轴标签/图例可读、三线表完整、页码 1/2 连续。摘要样例较短的留白符合其工具链测试用途。
- test_nohash_adapter.py：8 项测试全部通过，退出 0。覆盖未变化资源、mtime 漂移、源文件缺失、未绑定资源、源码变动、发布 PDF 绑定、过期源码记录、版本解析。临时副本已由测试清理，权威代码/数据/结果未修改。
- Compress-Archive、Expand-Archive 与解压后的 pdfinfo：均成功。export_smoke_bundle.zip 解压至 smoke_unpacked，4 个文件大小与已知来源相符；PDF 可正常解析为 A4、2 页、213730 bytes，无加密。未调用任何摘要或校验和检查命令。

全部构建使用 paper_tools/latex_paper_nohash.py，不执行技能原版的摘要计算。记录仅基于路径、大小、mtime、参数、命令和实际打开结果，不能证明大小与 mtime 均未变化时的内容一致性。

完整论文仍须等待独立 P2 及 W1，通过后再写作、启用完整质量门槛、编译和执行 W2。用户已有 paper/ 与 submission/ 不修改。

## 完整代码附录样例

2026-09-11 01:10 左右补充：code_export_smoke 从同一内置模板初始化，以 fvextra 的 VerbatimInput 导入完整 782 行 microgrid_core.py 副本。嵌套样例项目的资源范围仅为 paper_tools，因此先复制到 code_smoke_inputs，再建立大小/修改时间绑定；没有修改原始核心程序。两次尝试将项目范围外的核心文件直接绑定被工具安全拒绝，调整为范围内副本后 bind 退出 0。

- build 退出 0，零 warnings/issues，PDF 为 105614 bytes、20 页（第 1 页样例说明，第 2—20 页代码附录）。
- validate 退出 0，不启用完整论文质量门槛；A4、无空白页、字体全部嵌入、无位图。正文/附录边界参数仅用于验证样例页段识别。
- pdftoppm 渲染第 2、3、20 页退出 0；实际打开三页，确认中文字符串、行号、缩进、长行折行及末尾代码完整可读，未超出页边。
- 本样例只证明完整代码导出可行，不构成论文正文、W1/W2 或完整交付。
