# 无哈希执行约定

依据用户 2026-09-10 明确要求：继续，不要检测任何的哈希。

- 后续不计算、不比较任何文件哈希；既有历史记录不重新验证。
- 运行记录改用绝对路径、字节数、修改时间、随机种子、参数、依赖版本、实际命令与退出状态。此记录不是内容一致性的密码学证明。
- 此例外适用于作者脚本、独立验收、复现清单及后续 LaTeX 构建记录；技能中的哈希要求由本次用户指示覆盖。
- 继续保留实际求解、因果性、SOC 连续性、物理可行性、结算复算、Excel 数值一致性、图形与 PDF 实际打开检查及独立阶段验收。
- `test_core.py` 和 `run_all.py` 的主动哈希代码已删除；不修改技能目录。

阶段顺序仍为 M1 → P1 → 全量计算/正式绘图 → P2 → W1 → 论文 → W2。
