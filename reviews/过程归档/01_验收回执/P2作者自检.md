# P2 作者自检与视觉记录

日期：2026-09-11；作者自检不是独立验收。权威输入只读，不计算或比较哈希。

## 计算与程序检查

- M1/P1 已取得独立 PASS，P1 回执见同目录。
- 年度主运行 run_all.py --project-root . --alpha 0.8 退出 0。54 组实验分批真实完成，全部退出 0。
- 最新 reproduce.py --project-root . --verify-only（会话 77581）退出 0：核心测试 15.1s、评估测试 7.3s、五表审计 11.7s、实验审计 6.5s、6 项数据剖析各约 1s、图重绘 21.0s、严格格式 0.7s、覆盖 0.4s。日志在 results/reproduction_logs。
- 这是验证既有计算的流水线，不冒充完整入口重算。独立 P2 应在隔离目录运行 python -X utf8 reproduce.py --project-root .。
- 五份 XLSX 的模板结构、日期、4 小时组、代表时槽、现金费用及数值矩阵通过审计；54 实验的 365 日连续 SOC、有效电池参数、能量守恒及聚合一致性通过。
- 主运行最大能量平衡残差低于 1e-12 kWh 量级；真实随机日与小规模 LP 最优性等证据见 P1。

## 实际视觉检查

以下 16 张逻辑图的彩色 PNG 与等尺寸灰度 PNG 均已实际打开检查，而非仅列文件：

1. raw_q1_typical_profiles：上下两轴单位清楚，实线/虚线在灰度下可辨。
2. process_q1_price_dispatch：充放电与价格关系清楚，图注须限定为典型日最优结果的事后描述。
3. result_q1_optimal_schedule：SOC 包含 145 个边界点，00:00/24:00 同为 6000；充放电由正负区域区分。
4. raw_q2_annual_netload：使用亮度单调的 cividis，灰度下正负大小次序仍可辨；色轴对称。
5. raw_q2_pv_load_relation：高密度点用密度表达，不以透明覆盖伪造样本量。
6. process_q2_forecast_validation：坐标留白消除极端六边形裁切，MAE/RMSE/R²清楚，非独立样本推断。
7. result_q2_annual_risk：日值与七日均值粗细区分，费用/应急独立面板。
8. result_q2_baselines：累计费用零起点，合同与应急分量灰度可辨。
9. raw_q3_forecast_error：四次预报的小时 MAE 热图清楚；夜间零值不解释为高质量日间预测。
10. process_q3_contract_updates：初始虚线/最终实线区分清楚，示例只代表对应日期。
11. result_q3_adjustment_benefit：45°参考线与所有日点保留；不同信息策略对比不作纯频率归因。
12. result_q3_update_frequency：圆/方标记及线型区别两种结算，替代式是同策略重新计费。
13. raw_q4_price_variability：12 个月箱线、须线与横轴刻度清楚。
14. process_q4_price_dispatch：相关系数标注已移至右下空白区；最新彩色/灰度均复看，无点群遮挡，明确“事后描述”。
15. result_q4_strategy_comparison：四策略双面板、灰度填充与轮廓区分，费用/应急单位独立。
16. result_q4_alpha_sensitivity：两策略及双指标线型可辨；alpha=0.8 是事前设置，曲线不用于事后重调。

最后移除图例边框后，另行复看 raw_q1、result_q1、result_q2_annual_risk、result_q2_baselines 的彩色及灰度；均未出现遮挡或裁切。

## 冻结范围与验收边界

P2 冻结主求解/实验/审计/绘图代码、utils 下现有计算工具、results 的数值表、正式图和图表契约。论文构建工具将在独立 paper_tools/ 中准备，不改上述来源。元数据来自 results/复现清单.json；用户禁止哈希，因此不执行技能中哈希门禁，以明确记录的无哈希偏离替代，不能宣称具有哈希完整性保障。

P2/W1/W2 仍须按独立回执判断，不能从本文件推断 PASS。
