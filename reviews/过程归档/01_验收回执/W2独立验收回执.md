# W2 独立验收回执

## 一、审查范围

本次以未参与论文写作的独立审阅者身份，对提交候选材料执行只读终审。除创建本回执外，未修改论文、结果、程序、图表、参考文献、构建记录或支撑材料。审查范围如下：

- 题面：`data/C题.pdf`。
- 论文源文件与成稿：`参考初稿-LaTeX/main.tex`、`参考初稿-LaTeX/sections/`、`参考初稿-LaTeX/references.bib`、`参考初稿-LaTeX/figs/`、`参考初稿.pdf`、`参考初稿.build.json`。
- 分析与术语材料：`题目分析报告.md`、`术语表格.md`。
- 正式结果：`results/核心指标.json`、`results/预测误差指标.csv`、`results/典型日汇总.csv`，以及 `results/experiments/` 下的关键汇总文件，重点包括 `实验总表.csv` 和 `更新频率比较.csv`。
- 前序独立验收与核验：`reviews/P2独立验收回执.md`、`reviews/W1独立验收回执.md`、`reviews/规则与文献核验.md`、`reviews/论文技术验收.md`。
- 支撑材料：`参考支撑材料.zip`，以及其只读核验副本 `reviews/reference_support_unpacked/参考支撑材料/`；其中包括 `AI工具使用详情.pdf`、核心代码和五个正式结果工作簿。
- 页面渲染：`reviews/paper_render/` 的全部 38 页论文渲染图，以及 `reviews/ai_usage_render/` 的全部 3 页 AI 使用详情渲染图。

审查重点包括：四问完整性；证据、关键数值、单位、统计口径、公式和文字的一致性；指定解释边界；A4、页边距、摘要、目录、身份信息、正文页数、AI 声明位置、图表引用与文献合规；支撑包可读性、核心代码、五个正式结果工作簿和 AI 使用详情的存在性。根据用户指示，附录未逐行收入全部代码不作为阻塞项，完整代码以支撑包为准。

## 二、方法与命令

本次只使用路径、文件大小、修改时间、进程退出码、结构与对象信息、数值读取及逐页视觉检查。全过程没有计算、比较或检测任何哈希、CRC 或其他校验和；因此本回执不提供密码学内容认证。

代表性只读方法与命令如下：

1. 使用 `pdftotext` 提取题面和论文文本，核对题目四问、论文结构、术语、数值、单位与限定语；题面文本提取退出码为 0。
2. 使用 `pdfinfo`、`pdffonts` 和 PDF 页面对象信息核对页数、A4 尺寸、加密、旋转与字体嵌入；同时以 PowerShell `Get-Item` 获取可靠的文件大小和修改时间。论文 PDF 为 38 页，A4，无加密、无旋转，字体已嵌入。
3. 逐页视觉检查 `reviews/paper_render/page-01.png` 至 `page-38.png`，并逐页检查 `reviews/ai_usage_render/page-1.png` 至 `page-3.png`，核对裁切、乱码、空白页、页码、图表可读性、摘要、正文、参考文献、附录和 AI 使用详情的版式。
4. 使用 `rg` 检查 LaTeX 文件结构、图表/公式/文献引用、目录命令和身份信息关键词；结合 `references.bib` 与构建记录核对引用键和未解析引用。
5. 使用 PowerShell `ConvertFrom-Json`、`Import-Csv` 只读读取核心指标、预测误差、典型日和实验汇总，逐项与正文、表格及结论进行数值和统计口径对照。
6. 使用 .NET `System.IO.Compression.ZipFile.OpenRead()` 只读打开 `参考支撑材料.zip`，逐个条目将内容流读取至 EOF。ZIP 共 75 个文件条目，声明解压总字节数与实际流式读取总字节数均为 21,013,055，读取失败 0；此过程不解压写入，也不读取、计算或比较任何校验和。
7. 使用 openpyxl 的 `read_only=True` 模式独立打开 ZIP 核验副本中的五个正式结果工作簿；五次打开均成功，命令退出码为 0。
8. 对照 `参考初稿.build.json` 检查构建过程、返回码、警告和问题列表，并按源文件、PDF、构建记录、渲染文件、AI 详情和 ZIP 的 mtime 检查产物先后关系。构建记录为 `passed=true`，构建返回码 0，`warnings=[]`，`issues=[]`。
9. 交叉参考 P2、W1、规则与文献核验、论文技术验收记录，但终审结论以本次对题面、源文件、PDF、正式结果、支撑包和全页渲染的独立复核为基础。

## 三、问题清单（P0/P1/P2）

### P0

无。未发现导致结论错误、材料不可用或无法提交的致命问题。

### P1

无。未发现四问缺失、关键数值冲突、统计口径混淆、公式与文字矛盾、正文超页、版式违规、支撑包损坏或正式结果缺失等提交阻塞问题。

### P2

1. **最终上传入口和临场规则仍需人工确认。** 已有规则核验支持 A4、页边距不小于 2.5 cm、摘要不超过 1 页、无目录、无身份信息、正文不超过 30 页以及正文和支撑包各不超过 20 MB；但最终上传入口、开放窗口、文件命名和平台侧校验状态仍应由参赛队伍在提交时以官方页面为准。该项不是当前材料的技术缺陷。
2. **AI 使用详情的工作区顶层定位与 ZIP 内定位不同。** 用户列出的顶层路径 `参考支撑材料/AI工具使用详情.pdf` 当前未作为独立解压目录存在；实际文件已确认存在于 `参考支撑材料.zip` 内，并在只读核验副本 `reviews/reference_support_unpacked/参考支撑材料/AI工具使用详情.pdf` 中成功打开、读取和逐页检查。该 PDF 为 3 页 A4、106,875 字节、无加密、版面完整，故不影响支撑包完整性或提交。
3. **最终学术责任和上传动作不能由技术验收替代。** AI 使用详情和论文均已说明，队伍仍须理解、核验并对最终文本、引用、结果和上传版本负责。此项属于提交前人工责任提示，不是 P0/P1。

附录仅提供代表性代码节选而未逐行收入全部代码，按用户明确口径不列为问题；支撑包内已有完整核心程序与复现入口。

## 四、已核对的关键事实

### 1. 四问与证据闭环

- 题面 `data/C题.pdf` 共 3 页；论文对四问均给出模型、计算结果、图表/表格证据、解释与结论。
- Q1：购电量为 59,482.698167 kWh，费用为 35,126.948941 元；论文明确首末 SOC 均为 6,000 kWh。
- Q2：2025 年 2 月 1 日至 12 月 31 日共 334 日，费用为 18,764,165.933422 元，应急购电为 578,081.543577 kWh。负荷 MAE/RMSE 分别为 722.570828/904.089941 kW。
- 经验 90% 区间的实际覆盖率为：负荷 83.110862%，PV 83.458084%；论文只报告经验覆盖表现，没有将其表述为概率覆盖保证。
- Q3：费用为 16,279,696.690461 元，应急购电为 115,458.145704 kWh；相对 Q2 的费用差额为 2,484,469.242961 元。
- Q3 中 2h 相对 6h：主结算节约 708,343.300205 元，替代结算节约 127,065.263052 元，增加 2,672 次更新，对应阈值 265.098540 元/次。
- Q4-2/Q4-3：费用分别为 19,254,498.439401 元和 16,735,118.324559 元；应急购电分别为 575,898.350834 kWh 和 115,938.211929 kWh；费用差额为 2,519,380.114843 元。
- Q4 中 2h 相对 6h：主结算节约 701,631.699219 元，替代结算节约 139,845.674828 元，对应阈值 262.586714 元/次。
- `典型日汇总.csv` 共 16 行，覆盖 q2、q3、q4_2、q4_3 四种方案和四个代表日；`实验总表.csv` 共 54 行。

### 2. 指定解释边界

- **2h 口径：**正文将 2h 数据表述为用最新官方预报与已观测前缀构成的因果合成临近估计，没有冒充新增官方预报。
- **Q2→Q3 与 Q4-2→Q4-3：**正文明确这些比较同时改变信息来源和更新规则，不能归因于单一更新频率效应；Q4 相应页面再次给出该限制。
- **替代结算：**正文将其定义为同一物理策略按另一计费公式重计，不是重新优化或生成另一套调度策略。
- **90% 区间：**只报告经验区间及其实际覆盖率，不作 90% 概率覆盖保证。
- **334 日与全年图：**正式统计期为 2025-02-01 至 2025-12-31，共 334 日；1 月仅用于历史启动和 SOC 连续推进。原始年度图可覆盖 365 日，但正文已将全年原始图口径与正式 334 日统计口径区分。
- **结算结论边界：**正文说明主结算与替代结算下的排序和结论可能不同，没有将一种口径的结论无条件外推到另一口径。

### 3. 公式、引用、图表和构建

- LaTeX 源文件与 PDF 中的变量、单位、统计期和主要公式叙述一致；未发现与正式结果相冲突的关键公式或文字。
- 源码包含 12 个 figure 环境和 20 个编号公式环境；构建后图表与公式均可见、可读，正文引用关系未见断裂。
- 论文使用的 5 个引用键与 `references.bib` 中 5 个条目一致：`hu2021mpc`、`liang2014survey`、`raimondi2018twolayer`、`silvente2015rolling`、`zhao2020dayahead`。构建记录无未解析引用警告。既有规则与文献核验对 5 篇 DOI 元数据获得成功响应；该结果仅证明元数据核对，不等同于全文内容核验。
- `参考初稿.build.json` 记录构建通过，返回码 0，无警告、无问题；源文件、PDF、构建记录及渲染文件的修改时间顺序合理。

### 4. 版式与规则

- `main.tex` 明确使用 A4、12pt 和四边 2.5 cm；PDF 页面对象与视觉渲染相符。
- 摘要独占第 1 页；无目录；未发现参赛者、学校、指导教师等身份信息。AI 声明中的“参赛队伍”属于责任边界表述，不是身份信息。
- 参考文献自第 23 页开始，附录自第 24 页开始，因此正文共 22 页，满足正文不超过 30 页。PDF 总页数 38 页。
- AI 工具使用声明位于参考文献之前。
- 全部 38 页论文渲染未见裁切、乱码、空白页、页码异常或不可辨识图表；字体嵌入，PDF 无加密、无旋转。
- `参考初稿.pdf` 为 1,975,577 字节；`参考支撑材料.zip` 为 8,960,580 字节，二者均低于 20 MB。

### 5. 支撑包与可复现材料

- `参考支撑材料.zip` 可由 ZIP API 正常打开，75 个文件条目均可完整流式读取至 EOF，失败数为 0。
- 支撑包顶层包含 `AI工具使用详情.pdf`、`code/`、`data/`、`figures/`、`results/`、`utils/`、`README.txt` 和 `requirements.txt`。
- 核心代码存在，包括 `code/microgrid_core.py`、`code/run_all.py`，以及 `reproduce.py`、`run_experiments.py`、`plot_all.py`、测试和审计脚本。
- 五个正式结果工作簿均存在并可只读打开：`results/result1.xlsx`、`results/result2.xlsx`、`results/result3.xlsx`、`results/result4-2.xlsx`、`results/result4-3.xlsx`；工作表数依次为 2、3、4、3、4。
- AI 工具使用详情存在于支撑包中。其 3 页内容覆盖工具与使用范围、各阶段用途和采纳状态、主要交互示例、采纳/未采纳建议、待队伍人工核验清单及责任边界。
- P2 验收记录显示全量复现退出码为 0，640 项独立数值/物理检查、36 组因果扰动、54 组实验和五个工作簿均通过；W1、规则与文献核验、论文技术验收的结论与本次只读终审未出现实质冲突。

## 五、剩余人工动作

1. 参赛队伍在提交前从官方入口再次确认上传窗口、文件命名、文件数量、大小限制、AI 声明要求和平台校验提示，并以当时官方页面为最终依据。
2. 用实际待上传文件再做一次人工目视确认：正文 PDF 为本次审阅的 `参考初稿.pdf`，支撑文件为本次审阅的 `参考支撑材料.zip`，避免误选旧版本或只读核验副本。
3. 队伍成员应亲自通读摘要、四问结论、局限、参考文献和 AI 使用详情，确认能够解释 2h 因果合成口径、联合变化比较、同策略替代结算、经验区间覆盖边界以及 334 日正式统计口径。
4. 在官方平台完成上传后，人工检查平台回显的文件名、页数、大小和预览，并保存提交成功凭证。

上述均为正常提交动作，不改变本次技术终审结论。

## 六、结论（PASS/FAIL/BLOCKED）

**PASS。**

未发现 P0 或 P1。四问内容完整，关键证据与数值、单位、统计口径、公式和文字保持一致；指定的因果合成、联合变化、同策略重计、经验覆盖和 334 日/全年图口径均有清楚边界；正文规则、版式、引用、AI 声明和页数通过；支撑包可读，核心代码、五个正式结果工作簿和 AI 使用详情齐全。现有 P2 仅涉及最终平台人工确认、工作区路径定位说明和参赛队伍的最终责任，不阻塞提交。
