# P2 作者自检（编程改进版）

## 状态

作者自检通过，等待独立 P2；本文不替代独立验收。

## 冻结对象

- 代码：`microgrid_core.py`、`run_all.py`、`run_programmer_v2.py`、`evaluate_results.py`、`audit_results.py`、`plot_all.py`、`reproduce_programmer_v2.py`、`test_core.py`。
- 结果：`results_versioned/programmer_v2_final/`。
- 图：`figures_versioned/programmer_v2_final/`。
- 复现清单：`results_versioned/programmer_v2_final/复现清单.json`。
- 评审回应：`reviews/编程改进意见回应.md`。

## 清单逐项核对

| 项目 | 结论 | 证据 |
|---|---|---|
| 输入和建模手文档只读 | 通过 | 新产物均写入版本化目录；未覆盖题目附件、`题目分析报告.md`、`术语表格.md` |
| 动态依赖检查 | 通过 | 实际只检查 data、visualization、optimization、excel；numpy 2.4.1、pandas 3.0.0、matplotlib 3.10.8、scipy 1.17.0、openpyxl 3.1.5 均可用 |
| 程序真实运行 | 通过 | 正式全年运行退出码 0，耗时 2417.56 秒；五个工作簿和逐槽结果真实生成 |
| 约束、边界和量纲 | 通过 | SOC 为 1,200—10,800 kWh；最大平衡残差约 `9.1×10^-13 kWh`；费用单位元、电量 kWh、功率 kW |
| 求解稳健性 | 通过 | LP 为确定性模型；无 NaN/inf；求解失败有延迟贪心回退；边界、非前视、零方差和极端缺口定向测试通过。多初值/多种子不适用于确定性线性规划 |
| P1 | 通过 | 先前独立 P1 为 PASS；冻结候选又执行 `p1_readonly_check.py --section all`，退出码 0，全年 52,560 槽检查通过 |
| 种子和参数 | 通过 | 种子 2026；F1、W30、alpha 0.8、unclustered、MPC/H36、tracking 0.01 均写入核心指标和复现清单 |
| 复现版本记录 | 按用户约束通过 | 用户明确要求不计算哈希；改记绝对路径、文件大小、修改时间，并披露没有密码学完整性保证 |
| 数据剖析与图表契约 | 通过 | `原始数据剖析.csv` 与 `figures/图表契约-重算版.md` 记录样本量、缺失、分布、异常、单位和逐图结论 |
| 三类图和子问题覆盖 | 通过 | 13 张逻辑图；raw 5、process 4、result 4；q1—q4 每题三类均至少 1 张 |
| 图形语义与风格 | 通过 | 无 3D、双 Y 轴、rainbow/jet；颜色、线型和纹理有冗余编码；柱图零起点；统计量由代码计算 |
| 文件格式和尺寸 | 通过 | 13 对 SVG/PNG，PNG 约 300 DPI，SVG 有可编辑文字且无嵌入位图；灰度预览齐全 |
| 机械图审 | 通过 | `check_figure.py` 对 39 个彩色/矢量/灰度文件逐一 PASS；`figure_audit.py --questions q1 q2 q3 q4 --strict` 返回 `ok: true` |
| 人工读图 | 通过 | 逐张打开 13 张彩色图和 13 张灰度图；无缺字、裁切、遮挡，灰度可辨。Q4 的“节约 -0.6%”已改成“费用增加 0.6%”后重画重审 |
| 文件位置 | 通过 | 新文件均位于项目根目录，技能目录只读 |
| P2 | 待独立验收 | 作者自检结束后派发，PASS 前不声明编程手完整完成 |

## 实际命令与退出码

```text
python -X utf8 test_core.py                                      -> 0
python -X utf8 p1_readonly_check.py --project-root . --section all -> 0
python -X utf8 run_all.py ... --tracking-weight 0.01             -> 0
python -X utf8 audit_results.py ...                              -> 0
python -X utf8 plot_all.py ...                                   -> 0
python -X utf8 check_figure.py <全部 PNG/SVG/灰度 PNG> --strict  -> 0
python -X utf8 figure_audit.py ... --questions q1 q2 q3 q4 --strict -> 0
python -X utf8 reproduce_programmer_v2.py ... --reuse-results    -> 0
```

技能文档示例中的根目录 `scripts/check_env.py` 实际不存在，首次调用退出码 1；随后使用技能内真实路径 `references/roles/编程手/scripts/check_env.py` 重跑并返回 `ok: true`、退出码 0。该路径问题不影响模型程序。
