# 编程手交付说明

## 1. 最终模型

本轮已经把评审意见落实到可运行程序。冻结配置为：

```text
F1 周周期预测 + 30 日残差窗口 + alpha=0.8
+ 未聚类历史残差 + 36 槽滚动 MPC + SOC 跟踪权重 0.01
```

它只使用决策时刻已经发布的信息。真实负荷和光伏只用于执行动作裁剪，不会被提前送进优化器。应急购电只能补当槽缺口，不能给储能充电。

## 2. 主要结果

正式评价期为 2025 年 2 月 1 日至 12 月 31 日，共 334 天。

| 情景 | 现金费用/元 | 应急购电/kWh | 闲置合同/kWh |
|---|---:|---:|---:|
| Q2 固定价、无调整 | 14,890,440.41 | 304,774.78 | 1,442,540.28 |
| Q3 固定价、滚动调整 | 14,975,463.69 | 320,219.85 | 1,181,814.50 |
| Q4-2 分时价、无调整 | 15,667,460.85 | 303,418.23 | 1,434,537.98 |
| Q4-3 分时价、滚动调整 | 15,763,512.43 | 317,107.65 | 1,154,872.48 |

Q1 典型日计划购电量为 59,482.70 kWh，现金费用为 35,126.95 元。F1 在 2—12 月的负荷预测 MAE 为 176.80 kW、RMSE 为 244.29 kW；光伏预测 MAE 为 153.40 kW。

结果并不支持“合同更新必然省钱”：在当前题目结算规则下，Q3 比 Q2 多支出 85,023.28 元，Q4-3 比 Q4-2 多支出 96,051.58 元。更新减少了闲置合同，但调整溢价和预测偏差抵消了这部分收益。这是应当保留的模型结论，不是需要隐藏的异常。

## 3. 交付位置

- 核心程序：`microgrid_core.py`、`run_all.py`、`run_programmer_v2.py`、`plot_all.py`。
- 唯一复现入口：`reproduce_programmer_v2.py`。
- 正式表格和五个官方工作簿：`results/`。
- 正式图：`figures/`，共 13 张逻辑图，每张含 SVG、300 DPI PNG 和灰度预览。
- 对评审意见的态度：`reviews/过程归档/00_评审意见/编程改进意见回应.md`。

## 4. 唯一复现命令

在项目根目录运行：

```powershell
python -X utf8 reproduce_programmer_v2.py --project-root . --output-dir results --figures-dir figures
```

该命令依次重算全年结果、核对官方工作簿、重画全部图，并运行单图合规和 q1—q4 覆盖审计。

## 5. 验证边界

作者检查已覆盖 SOC 上下界与跨日连续性、母线平衡、同时充放电、应急电充电、Q3 发布时间、NaN/无穷值、Excel—CSV 一致性以及应急区间合并。按用户要求没有计算文件哈希，清单只记录绝对路径、字节数和修改时间；因此可以追踪本机版本，但不具备密码学防篡改能力。

这些内容是竞赛研究草稿，队伍仍需在提交前人工核对、理解并改写。
